# Instrucciones para el Agente
	
> **CLAUDE.md es la ÚNICA fuente.** `AGENTS.md` y `GEMINI.md` son **copias generadas** (la misma doctrina carga en cualquier entorno de IA). **Edita SOLO `CLAUDE.md`** y corre `sync_docs.ps1` (o `sync_docs.sh`) para regenerar las copias **antes de commitear**. No edites AGENTS.md/GEMINI.md a mano: se sobrescriben.

---

## ALEPH — Constitución del producto (v3 · MIGRACIÓN · jun-2026)

> **DOCUMENTO GOBERNANTE.** Léelo antes de cualquier trabajo de ALEPH. Reemplaza como norte estratégico a los planes Streamlit-internos previos (`app_factibilidad/NORTE_TABLEROS.md`, `REESTRUCTURACION.md`), que pasan a ser **contexto histórico**. Las **cifras doradas** y los **gotchas operativos** viven en «Aprendizajes del Agente» (abajo) y siguen vigentes.

ALEPH es la plataforma de evaluación financiera de proyectos inmobiliarios de CG Constructora S.A.S. DECISIÓN ESTRATÉGICA: migrar a arquitectura profesional de 3 capas por **ESTRANGULAMIENTO PROGRESIVO**. La migración se hizo SIN big-bang (la app Streamlit siguió en producción hasta tener paridad módulo a módulo). **Streamlit fue RETIRADO el 2026-06-15** (prototipo cumplió su ciclo): el stack productivo es **engine + api + web** únicamente. El único módulo sin paridad en /web al retirarlo (Monitor/EVM) preservó su dato (`data/navarra_data.py`) pendiente de los `actuals`/SINCO. Cada fase termina con algo desplegado y usable.

### Arquitectura objetivo (monorepo)
```
/engine   → `aleph_engine`: paquete Python PURO. Toda la lógica financiera.
            Sin imports de Streamlit/FastAPI/Supabase. Modelos Pydantic.
            Cobertura de tests obligatoria para toda función financiera.
/api      → FastAPI. Expone el motor y los datos (Supabase/Postgres).
            Contrato OpenAPI versionado. Auth: valida tokens de Entra ID
            (mismo tenant que hoy). Autorización por rol.
/web      → Next.js (App Router) + TypeScript + Tailwind + shadcn/ui.
            SOLO presenta: ningún cálculo financiero en el frontend.
            Gráficos: Recharts (o Tremor). Auth: NextAuth con proveedor
            Microsoft Entra ID.
/data     → JSON de proyectos (ilustrativos commiteados + REALES gitignored)
            + datos operativos (navarra_data.py). Los lee la API (fallback
            local) y los tests del motor.
(app_streamlit RETIRADO 2026-06-15: prototipo cumplió su ciclo. Antes de
 borrarlo se extrajeron: el dorado → engine/tests/golden/, los datos → data/,
 las anclas + el regenerador → engine/tests/ y engine/execution/.)
```

### Reglas innegociables de la migración
1. **SNAPSHOT DORADO AUTOMÁTICO:** antes de extraer cualquier cálculo, generar tests que congelen los resultados actuales de Navarra Apartamentos (TIR proyecto 37.60%, VPN @TIO 18.3 mil M, TIR socio 41.72%, ventas 229.7 mil M, costo directo 143.5 mil M, exposición máx −71.0 mil M, crédito máx 49.3 mil M, y el flujo mensual completo). La migración no valida estas cifras: garantiza que NO CAMBIEN. Desviación > 0.1% rompe el build.
2. **PARIDAD ANTES DE APAGAR:** un módulo de Streamlit solo se retira cuando su equivalente en /web está desplegado y verificado. *(Regla cumplida; Streamlit retirado 2026-06-15. Excepción consciente: Monitor/EVM se retiró sin paridad en /web pero preservando su dato, pendiente de actuals/SINCO — ver aprendizajes.)*
3. **SUPABASE:** única base de datos de ambas UIs durante la transición. Revisar `list_tables` antes de toda migración de esquema. Migraciones versionadas. RLS por rol (admin edita, gerencia lee).
4. **DEPLOY DETERMINISTA:** imágenes Docker etiquetadas con SHA del commit (nunca `:latest`). /api en Azure App Service (ACR `cgfactibilidadacr`, imagen `alephapi:<SHA>`); /web en Vercel. Versión visible en el footer de /web y en `/version` del API.
5. **El usuario (Martin) NO es desarrollador:** cada fase debe incluir instrucciones de verificación manual simples (qué abrir, qué comparar) y scripts (`deploy.sh`, `dev.sh`) para no depender de comandos memorizados.
6. **DISCIPLINA MULTI-AGENTE (varios agentes/sesiones tocan este repo a la vez: VS Code + Cowork/cloud).** Sin coordinación esto causa trabajo pisado, `index.lock` colgados y estado inconsistente (ya pasó). Reglas:
   - **(a) Rama por módulo/agente. NADA de varios agentes commiteando a `main` a la vez.** El trabajo de feature va en su **rama** → **PR** → Martín mergea con el **gate dorado verde**. `fetch`/`pull` ANTES de arrancar; verificar que no hay otro agente activo en `main`.
   - **(b) El trabajo que MUEVE CIFRAS del dorado (estilo M1/M2) NO se aplica en silencio:** se prepara el **acta de re-baseline** (cada cifra + delta) + el test de diff, y se **ESPERA la aprobación EXPLÍCITA de Martín ANTES de aplicar/commitear** el cambio. **PROHIBIDO escribir "Aprobada por Martín" en un acta sin que Martín lo haya aprobado de verdad** — eso falsea la gobernanza.
   - **(c) Commit por módulo + push apenas termina** (no dejar trabajo sin commitear → evita locks colgados y que otra sesión lo pise). Working tree limpia entre módulos.
   - **(d) `index.lock` colgado:** confirmar que NO hay proceso git activo (`Get-Process git,git-remote-https`) y que el lock es viejo/vacío ANTES de quitarlo con `Remove-Item .git\index.lock`.

### Dominio financiero (no simplificar)
- Modelo mensual en COP corrientes. **Doble flujo SIEMPRE:** proyecto (desapalancado) e inversionista (apalancado), cada uno con indicadores.
- Esquema colombiano: separación + cuotas fraccionadas + contra escritura; recaudo en fiducia hasta punto de equilibrio; crédito constructor con desembolsos contra avance (~80% costo obra), intereses, amortización por subrogaciones.
- WACC build-up Damodaran (beta desapalancada→reapalancada, EMBI, paridad de inflación) y curva S de Gauss: **YA EXISTEN en el código Streamlit — extraer tal cual, no reescribir de memoria.**
- Indicadores mínimos: VPN, TIR proyecto, TIR inversionista/socio, múltiplo de equity, margen sobre ventas, yield on cost, incidencia del lote, costo directo/m², exposición máxima de caja y su mes, punto de equilibrio, payback.

### Gobernanza de cifras
- Diccionario único de indicadores en engine (`metrics.py`): clave, nombre, ETIQUETA DE BASE, definición, función. UI consume solo de aquí vía API.
- **Ninguna cifra sin etiqueta de base:** "TIR proyecto", "TIR apal. ref.", "TIR socio" — nunca "TIR" a secas. Proyectos greenfield: flag `is_greenfield` → mostrar "— greenfield", jamás TIR −99%.
- Checks de cuadre calculados en el motor y expuestos por la API: P&G suma al total, recaudo = ventas, flujo final ≈ utilidad, crédito cuadra, SPI plausible (0.4–2.0). La UI muestra el estado de checks en cada módulo.

### Modelo de datos objetivo
`companies` → `projects` (con fase del ciclo de vida: prefactibilidad, factibilidad, aprobado, preventas, construccion, entregas, liquidacion, cerrado) → `scenarios` (versionados: draft/approved/baseline; snapshot JSONB inmutable al aprobar; un solo baseline por proyecto) → `results_cache`. `actuals_*` para ejecutados (campo `source`: manual|excel|erp|crm). `audit_log` de todo cambio de supuestos. El JSON actual de cada proyecto se migra como escenario v1 approved.

### Diseño visual (la app entra por los ojos — nivel Linear/Mercury/Tremor)
- Design tokens centralizados (`web/styles`): base zinc/slate, acento teal CG #0E5E59 (tomar el exacto del logo), ámbar CG secundario, semántica verde/ámbar/rojo SOLO para estados. Dark y light mode desde el día uno.
- Tipografía: Inter (o Geist). `font-variant-numeric: tabular-nums` en TODO número. Cifras alineadas a la derecha. `fmt_cop()`/`fmt_pct()` es-CO únicos (miles con punto), compartidos vía librería del web.
- Componentes canon: `KpiCard` (valor + etiqueta de base + delta + estado), `ChecksBadge`, `PhaseBadge`, `DataTable` (densa, ordenable, export), `ChartCard` (gráfico + fuente + corte de datos). Construir UNA vez, reutilizar SIEMPRE.
- Layout: sidebar colapsable con grupos rotulados PROYECTO / PORTAFOLIO, breadcrumb Portafolio › {Proyecto} › {Módulo}, header permanente con Proyecto · Fase · Escenario vN (estado) · Corte de datos.
- Densidad financiera: mucha información, cero decoración. Animaciones sutiles (150-200ms). Estados de carga skeleton, nunca spinners gigantes.
- Accesibilidad AA de contraste. Responsive: dashboards usables en tablet (gerencia los verá en reuniones).

### Definición de "terminado" (toda fase)
Tests del motor verdes (incluido snapshot dorado) · checks de cuadre expuestos · ninguna cifra sin etiqueta en pantallas nuevas · desplegado con tag de commit y verificado en el footer · instrucciones de verificación manual entregadas a Martin.

---

## Aprendizajes del Agente (Mejora Continua)

> **INSTRUCCIÓN CRÍTICA — LEER PRIMERO:** Esta sección es tu memoria persistente de mejora continua. **Con cada ciclo de ejecución** (al completar una tarea, resolver un error, descubrir un patrón, o ajustar un flujo) **y con cada actualizacións de cualquier Markdown** (directivas, CLAUDE.md, AGENTS.md, GEMINI.md, READMEs de scripts), **debes agregar aquí un aprendizaje nuevo** si surgió algo no trivial. El objetivo es que este archivo se vuelva más útil y preciso con el tiempo, acumulando conocimiento del proyecto que no se pierde entre sesiones.
>
> **Qué registrar:** restricciones de APIs descubiertas, rate limits reales, patrones que funcionan, errores que se repiten, decisiones de diseño tomadas con el usuario, supuestos que resultaron falsos, atajos útiles, gotchas del entorno.
>
> **Qué NO registrar:** detalles efímeros de una sola tarea, información ya documentada en la directiva correspondiente, cosas triviales derivables del código.
>
> **Formato de cada aprendizaje:**
> ```
> - **YYYY-MM-DD — [Tema corto]:** Descripción del aprendizaje en 1-3 líneas. **Por qué importa:** consecuencia práctica o cómo aplicarlo en el futuro.
> ```
>
> **Higiene:** si un aprendizaje queda obsoleto o se contradice con otro más reciente, actualízalo o elimínalo en vez de acumular ruido. Mantén la lista ordenada por fecha (más recientes arriba). Si superas ~25 entradas, consolida las más antiguas o promuévelas a la directiva que corresponda.

### Registro de aprendizajes

- **2026-08-09 — Re-baseline del DORADO (Navarra) con un `par` que llegó recalibrado de otra sesión: el protocolo completo + 3 gotchas del gate:** Llegó un `par` de Navarra recalibrado contra el Excel de factibilidad, muy bien documentado (notas de procedencia, `_conciliacion_motor_excel`, `directos_cap`). **REGLA CONFIRMADA: por bien documentado que venga, un par que toca el dorado NO se aplica en silencio** (regla #6b) — se corre el delta, se **separan las DECISIONES de las EXTRACCIONES**, y se espera el OK explícito. Aquí eso valió: el paquete mezclaba mejoras de fidelidad legítimas con **dos decisiones disfrazadas de dato** que Martín acabó excluyendo — (a) **`wacc.inf_col` 5,1→6,4**, un input **MACRO común a los 8 proyectos**: moverlo solo en Navarra deja **dos WACC para el mismo entorno macro** (rompe el mapa de valor y el EVA consolidado, contradice /fuentes) y la fuente citada era **ICCV/ICOCED (costos de construcción), no IPC** — mientras el propio JSON citaba «BanRep inflación 4,8%» en otro bloque (**contradicción interna = señal de alarma**); (b) **`vmes` 15→20** en una etapa **sin licencia ni ventas abiertas**, siendo 20 el más optimista de tres números que el propio `_nota` listaba (Excel declara 12, histórico real ~9,2) **y a la vez declarando «escenario de ritmo pendiente»**. **PATRÓN DE ANÁLISIS que hizo la diferencia — descomposición por factor:** aplicar cada grupo de cambios **AISLADO** sobre la base y verificar que **la suma de los grupos reproduce el par completo exacto**; sin eso no se sabe qué cifra mueve qué y no se puede aprobar parcialmente. Reveló que la **reclasificación del local comercial** (de «otros ingresos» a ventas, +1,374%) **no cambia el total de ingresos** (es reparto entre líneas, no monto) y que el driver real de la exposición (−71,0 → −89,6 mil M) es el **recaudo** (`pct_ci` 30%→9,18%, separación 5.000→1.000), no las ventas. **GOTCHA 1 — `diff_dorado.py` en modo *recompute* NO SIRVE para un cambio de DATO:** recalcula desde el **`input_par` congelado en el snapshot**, así que un cambio en el JSON de `data/` es invisible → **modo dos-carpetas** (archivar snapshots → regenerar → comparar con el allowlist). **GOTCHA 2 — el regenerador reescribe el ORDEN de las claves** de los 3 snapshots ilustrativos **commiteados** (mismos valores, `git diff` con ruido) → **restaurarlos** (`git checkout --`) para que ese churn no entre al commit; y verificar **semánticamente** (no por bytes) que solo cambió el snapshot que debía. **GOTCHA 3 — hay GOLDENS FUERA de `test_anclas.py` y del snapshot:** dos tests clavaban cifras after-tax de Navarra (`test_vehiculos_tributario._GOLDEN_NAVARRA` y la aserción `iva_vis_devolucion > 0` de `test_tributario_after_tax`) → **un acta que solo lista anclas + rutas del snapshot SUBDECLARA**; correr la suite completa ANTES de dar el alcance por cerrado. Ese segundo test se reescribió para cubrir **los dos regímenes** (con `iva_en_operativo`: exige `== 0` **y** `recon_codensa > 0`; sin él, `> 0`) — queda más fuerte que la aserción original. **HALLAZGO sustantivo:** `iva_en_operativo` corrigió un **doble conteo real del IVA** en Navarra (estaba en `recon_codensa` **y** la capa after-tax lo volvía a sumar) → VPN after-tax −2,27 → **−11,79 mil M**, TIR proyecto after-tax 13,71% → **9,31%**. **No empeora el negocio: estaba sobreestimado** — pero es exactamente el tipo de cifra que hay que **avisar antes** de que aparezca en pantalla. Resultado: PR #105, gate **0 colaterales** (1.544 esperados), engine 198 / api 92 / ruff / next build verdes, y las **tres cifras de decisión intactas** (37,5975% · 18.280.688 · 41,7189%) porque salen del override `fiducia`. **Por qué importa:** queda el molde para cualquier re-baseline futuro del dorado (descomponer por factor → separar decisiones → gate dos-carpetas → suite completa → acta con TODO lo que se mueve → aprobación) y la lección de que **un par «solo de datos» puede traer decisiones de modelo escondidas**. Detalle en `docs/acta_rebaseline_navarra_20260809.md` y en la memoria `navarra-rebaseline-ago2026.md`.
- **2026-06-25 — Subir un proyecto al tablero DESDE el Excel del cliente (Argos → concurso público VIS CVP/RenoBo) + fix tributario after-tax VIS + gotchas de deploy/Monte Carlo. LECCIÓN MAYOR: "prima el Excel, NO re-modeles".** Argos (Fontibón) dejó de ser No-VIS: ahora es un **concurso público CVP/RenoBo de vivienda VIS/VIP** (suelo de Hábitat **pagado en unidades VIP** en especie; socio **Conaltura 50/50**, NO Coninsa; el TdR de Argos **aún no sale** — la plantilla es el predio precedente "Lomas Pijaos II", la matriz interna de CG lo confirma). **MI ERROR (la lección):** ante el Excel de prefactibilidad de CG, **re-precié** a topes SMMLV (ventas 297 mil M en vez de las **459.5 reales**) + usé **cronograma ajeno** (clonado de Navarra, lote upfront) → TIR 11% "destruye valor". AMBOS falsos; Martín lo cazó. **REGLA: cuando la fuente es el Excel del cliente, REPRODÚCELO fiel, no impongas supuestos.** **CÓMO reproducir un Excel en ALEPH:** (a) **P&G** → calibrar `costos_pct` a los % EXACTOS del Excel; OJO la **devolución de IVA VIS el Excel la mete en INGRESOS operativos** (por eso la utilidad operativa la incluye) → modelarla como "otros ingresos" vía `recon_codensa` (= IVA% + Codensa%), NO en la capa after-tax (si no, doble conteo). (b) **TIR/VPN** → extraer el **flujo de caja real del Excel** y usarlo como override `fiducia.fcl_proyecto` (igual que Navarra; el motor lo toma como "auditado" y la TIR/VPN salen del Excel, no del cronograma recalculado). GOTCHA: la hoja `FC INV ANUAL` estaba **rota (#REF!)**; el flujo vivo estaba en `FC INVERSION` (fila "Comprobacion" = FCL del proyecto, TIR 29.85%); se **anualiza** (suma por 12) porque la fiducia del motor es anual. Resultado: P&G y TIR (30.44% ≈ 29.85%, VPN +20) **calzan con el Excel** → **genera valor** (TIR>WACC). El socio CG sale TIR=None (en el deal CG no aporta capital → retorno por honorarios, IRR indefinida). **FIX TRIBUTARIO after-tax (engine, ADITIVO opt-in, dorado intacto):** la capa C1 **doble-contaba el IVA** (lo sumaba aunque ya estuviera en operativo) y **recalculaba sobre la serie interna** (divergía del titular auditado, p.ej. titular 30.44% vs after-tax 13.02%). Fix: flag `financiero.iva_en_operativo` (no re-suma IVA) + para un **VIS exento de renta** (renta_pg=0) con IVA en operativo → **sin impacto tributario neto** → after-tax = titular. **NORMATIVA COLOMBIA VIS (verificada con fuentes; [VALIDAR asesor]):** renta **EXENTA** por fiducia (ET 235-2 num.4 literales a/b **sobrevivieron** la Ley 2277/2022 — derogó solo c/d; **plena** para personas jurídicas, sin tope del 40%) + **devolución de IVA = 4%** del valor de escrituración (ET 850 par.2 / Dec.96/2020; el motor usa 3.8% conservador) + **GMF exento** en fiducia de vivienda (ET 879). RIESGO a confirmar con asesor: Argos es **renovación urbana** y la Ley 2277 derogó justo el literal **c** (utilidad de predios en renovación urbana) → confirmar que la utilidad del DESARROLLO VIS cae en a/b (vigente) y no en c. **HERRAMIENTAS/PATRONES nuevos:** `db/push_proyecto.py` (sube un proyecto a Supabase: projects + scenario approved + results_cache + audit; DRY-RUN por defecto, `--apply`, `--check-only`, idempotente por hash, gate schema+calcular+checks; slug prod con sufijo `_REAL` — OJO el slug LOCAL pierde el `_REAL` por el repo). **Banner de advertencia** en la ficha: `build.project` expone `disclaimer` (de `par["disclaimer"]`) y el web lo lee de `project.disclaimer ?? project.params?.disclaimer` → aparece en prod con SOLO el deploy del web (sin esperar redeploy del API, porque el par viaja en `params`). **GOTCHA `az acr build` (Windows):** crashea el CLI al streamear logs (`UnicodeEncodeError` cp1252 de colorama) **pero el build SIGUE en ACR** — NO re-lanzar a ciegas; verificar el estado con `az acr task show-run --registry <acr> --run-id <id>` y evitar el crash con `PYTHONUTF8=1`. **GOTCHA Monte Carlo en prod:** el MC corre vía Server Action que **re-ejecuta el motor por corrida** (~24s a 3.000 en Argos, ~34s en Navarra) → supera el timeout serverless de Vercel → error "An error occurred in the Server Components render"; fix `export const maxDuration = 60` en la ruta `/proyectos/[slug]` (máx de Hobby). **DEPLOY — corrige aprendizajes viejos:** **el agente SÍ tiene `az` en este entorno** (autenticado como `mgomez@cgconstructora.com`) → pudo **redeployar el API** él mismo: `az acr build` desde GitHub#main (tag=SHA, p.ej. `alephapi:6c04bec`) + `az webapp config container set ... --container-image-name` + `az webapp restart` (RG `Cg-factibilidad`, App `cg-aleph-api`); rollback = volver al tag previo (capturar `DOCKER_CUSTOM_IMAGE_NAME` antes). El `/web` auto-despliega en Vercel al mergear. **Por qué importa:** queda el molde completo para subir un proyecto-desde-Excel al tablero (reproducir P&G + flujo auditado vía fiducia, **NO re-modelar**), el fix reutilizable del after-tax VIS, la doctrina tributaria VIS-fiducia con sus `[VALIDAR]`, y tres gotchas de operación (az cp1252, MC timeout/maxDuration, push tool). Argos-CVP quedó en prod (escenario **v3**, etiquetado PROVISIONAL). **Pendiente:** asesor fiscal (renta renovación urbana, GMF, IVA 4% vs 3.8%) + costos bottom-up reales de CG + el TdR oficial de Argos (vigilar SECOP II). Detalle de proyecto en la memoria `argos-cvp-renobo.md`.
- **2026-06-19 — Monte Carlo estilo Crystal Ball (web-only) + patrones reutilizables de gráficas ECharts y de verificación:** Se subió la vista Monte Carlo (ficha → Sensibilidad) a nivel Oracle Crystal Ball **solo presentación** (el motor M5 ya da percentiles/certeza/tornado/corridas crudas → **dorado intacto**, web auto-despliega). PR `feat/mc-crystal-ball`. **PATRÓN — tooltip en serie ECharts `custom`:** las series `custom`/`renderItem` **NO autogeneran tooltip** (causa del bug "no muestra nada al pasar el cursor"); hay que declarar `tooltip` y leer **`meta[dataIndex]` del closure** (no confiar en `params.value`, que es el dato crudo `[x0,count,x1]`) — mismo patrón que `pyg-waterfall.tsx`. Se precomputa `meta[]` por bin (`{x0,x1,count,pct,cumPct}`) para que el formatter no recalcule. **PATRÓN — banda sombreada:** `markArea` por coordenadas de eje (se re-escala sola) — requiere registrar `MarkAreaComponent` en `web/src/lib/echarts.ts` (no estaba); **GOTCHA TS:** `data: [[...]] as const` la vuelve **readonly** → incompatible con `MarkAreaComponentOption.data` (mutable); tipar la variable como `MarkAreaComponentOption` SIN `as const`. **PATRÓN — curva acumulada:** serie `line` en un 2º `yAxis` (der, 0–100%), un toggle controla `showCumulative` (incluirlo en el dep-array de `useCallback(buildOption)` o no repinta; el wrapper `EChart` hace `setOption(...,true)` = reemplaza, así no quedan series fantasma). **DOCTRINA — "certeza interactiva":** mover un umbral y contar `valores.filter(v=>v>=u).length/n` EN VIVO en el cliente **NO viola "cero cálculo financiero en el front"** — es estadística DESCRIPTIVA sobre las corridas que el motor YA entregó (misma naturaleza que el binning); no re-corre la simulación ni toca TIR/VPN. Se etiqueta "(base X%)" + reset al valor del motor. **PATRÓN React — reset de estado al cambiar prop:** usar el patrón oficial de "ajustar estado EN RENDER" (`if (prevKey !== key) { setPrevKey(key); setX(...) }`) en vez de `useEffect(()=>setX(),[dep])` → evita el error de eslint `react-hooks/set-state-in-effect` y el render en cascada. **Detalle:** la etiqueta del marcador de MEDIA choca con P50 cuando son ~iguales → posicionar la de media abajo (`position:"start"`), el resto arriba (`"end"`). **VERIFICACIÓN de tooltips ECharts con agent-browser** (sobre `next start`, no dev): disparar `canvas.dispatchEvent(new MouseEvent('mousemove',{clientX,clientY,...}))` y leer el tooltip del DOM (`body.textContent` lo contiene); **seleccionar el canvas correcto** (`[...document.querySelectorAll('canvas')].pop()` = el último/sección final) — un `find` por texto devuelve un **ancestro** cuyo primer canvas es OTRO gráfico (me pasó: clientY negativo, fuera de viewport). Y esperar a que el Server Action termine (poll del botón "Re-correr") antes de cambiar de tab. **Fuera de alcance (Fase 2, requiere motor + datos de CG):** correlaciones Cholesky, VaR/CVaR, sesgo/curtosis, fitting. **Por qué importa:** quedan moldes para cualquier histograma/gráfica ECharts futura (tooltip en custom, markArea, eje secundario, toggle) + la regla de qué es "presentación" vs "cálculo" + cómo verificar tooltips de canvas.
- **2026-06-19 — Cableado el test de gobernanza "diff dorado viejo-vs-nuevo" (spec §2.3) — antes ad-hoc + GOTCHA cp1252 en CLIs Windows:** `engine/execution/diff_dorado.py` (+ `engine/tests/test_diff_dorado.py`, 11 tests) compara el dorado VIEJO vs el NUEVO y **FALLA si una cifra cambió fuera del allowlist del acta** → atrapa el **cambio COLATERAL oculto** que el harness ordinario, al regenerar snapshots, ya no vería. **Úsalo en el PRÓXIMO re-baseline (beta/IVA/after-tax) en vez de verificar a mano:** `python execution/diff_dorado.py --permite <cifras del acta>` — modo **recompute** (snapshot congelado vs `calcular()` ACTUAL, SIN regenerar; ideal tras tocar el motor, antes de regenerar) o modo **dos-carpetas** (archivo vs regenerado). El allowlist **ignora índices de lista** (`apalancamiento.flujo_equity` cubre `[*]`) y soporta prefijos de sección; misma tolerancia que el harness (0.1% rel / 1e-6 abs); también avisa "allowlist sin uso" (acta sobre-declara) y snapshots añadidos/faltantes. **Aditivo** (no toca `calcular()` → dorado intacto, engine **130** verdes). **Decisión de empaquetado:** la lógica pura vive en `engine/execution/diff_dorado.py` + `execution/__init__.py` para importarla desde `tests/` (pytest tiene `engine/` en `pythonpath`); `calcular` se importa **lazy** dentro del recompute, así los tests de lógica no dependen del motor. **GOTCHA REUTILIZABLE — consola Windows cp1252:** un CLI que **imprime** glifos no-Latin1 (`∅ ⚠ ✗ ✓ →`) crashea con `UnicodeEncodeError` (codec cp1252 de la consola), aunque el archivo fuente sea utf-8 (la fuente no se imprime, los `print()` sí) → fix: salida **ASCII** + `sys.stdout.reconfigure(encoding="utf-8")` defensivo (los acentos Latin-1 á/í/ñ/¿ SÍ pasan en cp1252; solo fallan los símbolos fuera de Latin-1). **Por qué importa:** queda la red que faltaba para que un re-baseline futuro NO enmascare cambios colaterales (cierra el "Pendiente relacionado" de `docs/acta_flujo_equity_20260614.md`); y la regla de hacer **ASCII-safe** cualquier CLI del proyecto (Martín corre en Windows).
- **2026-06-18 — Módulo de ONTOLOGÍA canónica (`engine/aleph_engine/ontologia.py`) + el patrón "fachada de solo-lectura" (aún MÁS conservador que una métrica aditiva: dorado verde SIN regenerar snapshots):** Se creó la fachada canónica que consolida en UN lugar el vocabulario del motor hoy disperso + nombra lo implícito + registra los invariantes de cuadre (cimiento del futuro "Auditor de presupuestos" y de la normalización del cotizador/SmartHome). **PATRÓN CLAVE — fachada de solo-lectura, UNA sola dirección:** `ontologia.py` **importa DE** los módulos existentes y re-exporta **por referencia** (cero valores duplicados que deriven); los demás módulos **NO** se reconectan a él (eso acoplaría comportamiento → paso futuro con golden propio). **DISTINCIÓN con la "métrica aditiva" (aprendizaje del 15-jun):** una métrica nueva añade un campo al `result` de `calcular()` → **rompe el harness dorado** (compara `set` de claves) y exige **regenerar snapshots** + verificar el diff. Esta fachada **no añade NADA al `result`** → el dorado pasa **sin tocar ni regenerar snapshots**; es el cambio más seguro posible. El test `test_dorado_intacto_la_fachada_es_aditiva` lo prueba (importar la fachada no mueve ninguna ancla) → **no requiere acta ni aprobación de cifras**. **Mapeo auditado (no asumido):** `FASES`→`config.ESTADOS`; `CATEGORIAS_COSTO`→`tuple(CostosPct.model_fields)`; `INDICADORES`→`metrics.REGISTRO`; `VEHICULOS`→`vehiculos.claves()`; `INVARIANTES`→cada uno apunta a `checks.correr`/`checks.check_spi` (funciones reutilizables, no inline — regla: si un check naciera inline, **documentar su ubicación, no extraerlo**). `TIPOS_PROYECTO=("VIS","VIP","No VIS")` se fija como **constante canónica** porque hoy `schema.Meta.tipo` es `Optional[str]` (sin fuente tipada). `HITOS`/`CONCEPTOS_RECAUDO` = constantes NUEVAS que solo **nombran** lo implícito (cronograma/recaudo colombiano), sin tocar el cálculo. **REGLA "no hornear suposiciones":** los ítems del glosario sin confirmar (capítulos del WBS bottom-up, % real del crédito constructor, uso de VIP, yield on cost) quedan como `# TODO [por validar]` **sin valor**, no como defaults inventados. **TEST de consistencia data-independiente:** un `R` sintético dispara los 5 checks de `checks.correr` y se verifica que sus `clave` == los `id` de los `INVARIANTES` (atrapa drift sin depender de los datos reales). **Verificado:** engine **119** verdes (incl. harness dorado, que CORRIÓ — los snapshots están presentes) + ruff limpio; 11 tests en `test_ontologia.py`; nota de mapeo en `docs/ontologia_canonica.md`. Rama `feat/ontologia` → PR (Martín mergea, con el gate dorado verde). **Por qué importa:** queda el molde "fachada canónica de solo-lectura, una dirección, dorado-verde-sin-regenerar" para cualquier consolidación de vocabulario futura; y la jerarquía de seguridad de cambios al motor: **fachada read-only (cero cambio al result) ⊂ métrica aditiva (campo nuevo, regenera snapshots) ⊂ cambio de cifra (acta + aprobación de Martín)**. **Próximo paso (NO este):** re-cablear `schema` para validar `Meta.tipo` contra `TIPOS_PROYECTO` acopla comportamiento → exige su propio golden. **Opcional:** versionar el glosario v0.1 `Ontologia_Canonica_CG_borrador.md` (OneDrive ALEPH) como `docs/ontologia_canonica_glosario.md`.
- **2026-06-18 — Pilar 2 (inteligencia de PORTAFOLIO) COMPLETO: 5 piezas ADITIVAS (tesorería #55 + asignación de capital #56 + estrés de tesorería #58 + concentración #60 + cabina del CEO #62) + el patrón de consolidación + cómo estresar sin romper el dorado:** Tras "más allá de un proyecto, ¿qué le añadirías como CEO?", se abrió **Pilar 2 = inteligencia de la EMPRESA entera** (no de un proyecto). Cinco piezas en 3 capas (engine→api→web), TODAS **ADITIVAS (dorado intacto, no tocan `calcular()`)**, rama→PR→Martín mergea, gate verde. **PIEZA 1 — Tesorería consolidada (`portfolio.tesoreria`):** alinea TODOS los proyectos en un **calendario común** y suma mes a mes la posición de caja + el saldo de crédito → la **necesidad máxima de caja combinada** (−221 mil M @ 2029) y el **crédito máximo combinado** (165 mil M). **PIEZA 2 — Asignación de capital (`portfolio.capital`):** por proyecto, **equity pico** (`|min(acum)|` = necesidad máx. de caja propia tras crédito), crédito máx, valor creado (EVA) y **eficiencia = valor/equity**, rankeado (Dominica 0.93 ≫ Navarra 0.21 ≫ Argos −0.24 destruye ≫ Torres greenfield). **PATRÓN CLAVE — consolidación de portafolio ADITIVA:** una función nueva en `portfolio.py` que recibe `items=[(slug,par,R)]` y **agrega las series/escalares que `calcular()` YA produjo** (`R["apalancamiento"]`: `acumulado`, `saldo_credito`, `max_necesidad_caja`, `credito_max`, `valor_creado`, `crea_valor`) NO toca el motor → **dorado intacto sin regenerar snapshots** (a diferencia de una métrica nueva DENTRO del `result`, que sí rompe el harness). El **alineado temporal** reusa el de `consolidar` (eje global **epoch-2022**, `off=(base.year−2022)*12+(base.month−1)` con `base=min(IV de hitos)`); la caja consolidada = cumsum del operativo desplazado (= suma de los `acumulado` alineados, reconcilia). **INSIGHT FINANCIERO que vende la feature:** el pico SIMULTÁNEO (crédito 165, equity −221) es **MENOR que sumar los picos individuales** (184.6 / 247.7) porque **no coinciden en el tiempo** → diversificación temporal = capacidad de financiación que no sabías que tenías; mismo mensaje en ambas piezas. **PATRÓN — reusar componente probado para la vista consolidada:** la gráfica de tesorería **reusa `CashFlowChart`** (el del flujo por-proyecto) alimentándolo con las series consolidadas (mapea `caja→acum`, `credito`, `exposicion_maxima→maxExposure`) — cero gráfica nueva. **GOBERNANZA — consistencia greenfield:** en `capital`, un proyecto con `crea_valor None` (TIR degenerada) → **sin VALOR ni eficiencia ("— greenfield")** aunque `valor_creado` (VPN@WACC) sí esté computado; esto alinea con la consolidación EVA del portafolio (`build.portafolio` excluye greenfield) → el footer de valor (+6.9 mil M) **coincide** con el ValorBanner consolidado. Equity/crédito SÍ se muestran para greenfield (válidos sin veredicto). **Recorte de ventana de tesorería:** la fase de DÉFICIT/financiación (`caja<0` o `saldo>0`), NO el nivel residual (un proyecto que termina en pérdida deja caja negativa PLANA que estiraría el horizonte a 18 años; con el recorte por déficit queda en ~10 años útiles). **GOTCHA ultracode/workflow:** lancé un workflow de 9 agentes para DECIDIR la pieza (4 mapean + 4 lentes + síntesis) pero **chocó con el límite de sesión** (se cayeron síntesis + las 4 lentes; "resets 11:30pm Bogotá") → `result=null`. **Lección:** cuando un workflow se bloquea por límite, **NO re-lanzar a ciegas** (re-gasta el límite); decidir+construir con criterio propio (tenía el conocimiento del motor de la pieza 1). **PIEZA 3 — Estrés de tesorería (`portfolio.estres_tesoreria` + `modelo.correr_estresado`, PR #58):** la gestión de RIESGO — si las ventas caen/se atrasan y suben los costos en TODA la cartera, ¿cuánto se profundiza el valle? Aquí la pieza NO es solo-agregación: hay que **RECALCULAR** cada proyecto con un shock y re-consolidar. **GOTCHA CRÍTICO:** `calcular()` NO sirve para estresar un proyecto con `tipologias` — su `normalizar_tipologias` (línea 514) **re-derivaría `ventas_miles` de la tabla SIN shock**, borrando el shock de precio. La vía correcta es **reusar la maquinaria del Monte Carlo** (`mc_trial` ya aplica el shock bien: escala `ventas_miles` DESPUÉS de normalizar y NO re-normaliza, + el fix de escrituración + ignora la fiducia). **PATRÓN — factorizar para una sola fuente del shock:** extraje `_correr_trial(ctx,dp,dc,dv)→(p,pg,hitos,recaudo,ap)` de `mc_trial` (que queda **bit-idéntico**, devolviendo solo escalares) + `correr_estresado(par,...)` que devuelve el `R` completo para re-consolidar; **pineado con un test de consistencia** (`correr_estresado` vs `mc_trial` en exposición/tir/vpn) que atrapa drift. La fiducia override **no afecta la serie de tesorería** (solo el fcl de decisión) → la base (calcular) y los escenarios (correr_estresado, sin fiducia) son apples-to-apples; la invariante DURA es shock-NULO=base. **HALLAZGO no trivial (rompió un test):** un shock uniforme **NO profundiza siempre el valle CONSOLIDADO** — un ritmo −30% **DESINCRONIZA** los troughs (y los picos de crédito) de los proyectos → puede REDUCIR el pico SIMULTÁNEO (efecto de timing/diversificación). En los reales profundiza (−221→−237, +16 de equity, y deja la empresa en −64 cuando la base ya recuperó); en el mix ilustrativo de CI se hizo más somero → **no asumir dirección del delta en los tests**. Web: overlay de la caja estresada (roja punteada) sobre la base (teal) con toggle de escenario; ventana = la de la BASE (el valle estresado cae dentro; la cola de recuperación lenta se omite). **PIEZA 4 — Concentración / diversificación (`portfolio.concentracion`, PR #60):** vuelve a la agregación pura — por DIMENSIÓN (proyecto, ubicación, tipo, fase) mide por VENTAS el share + el HHI (Herfindahl) + el número EFECTIVO de categorías (1/HHI). **GOTCHA de DATOS:** el `tipo` venía con casing inconsistente en los JSON (`"NO VIS"` vs `"No VIS"`) → sin canonizar parte la MISMA categoría en dos e **infla falsamente la diversidad**; se canoniza en el agrupado (VIS/VIP→"VIS", variantes de no-vis→"No VIS"), sin tocar los datos. Web: barras de proporción en **CSS** (no ECharts — es proporción, como la barra del WACC) con la **paleta de charts theme-aware** (`chartTokens(isDark).palette` + `useIsDark`), cue ámbar de marca si un líder pesa >50%. Hallazgo (reales): balanceado por proyecto/tipo pero **62% en Bogotá** (concentración geográfica). **PIEZA 5 — Cabina del CEO (`portfolio.salud`, PR #62):** la capa de SÍNTESIS — destila valor/EVA, concentración, resiliencia (un escenario de estrés severo) y eficiencia de capital en ALERTAS priorizadas (crítico/alerta/info). **PATRÓN — síntesis UI-AGNÓSTICA:** el motor reusa las otras funciones (`capital`/`concentracion`/`estres_tesoreria`) y emite alertas **estructuradas** (`{nivel, tipo (enum estable), datos}`) SIN texto en español; la web mapea `tipo`+`datos`→copy. Así el engine no carga strings de presentación (constitución). Reglas: crítico=destruye valor; alerta=concentración(líder>50%)+resiliencia(recesión severa deja la caja en déficit); info=greenfield+dónde rinde el capital. Web: lista con icono+tinte por severidad (**sin side-stripe borders** — prohibido; tinte de fondo). Hallazgo (reales): 1 crítica (Argos), 2 alertas (Bogotá 62%, déficit bajo recesión), 2 notas. **Por qué importa:** queda el molde para CUALQUIER vista de portafolio futura (función `portfolio.X(items)` aditiva que agrega series de `R` + endpoint thin `build.X` + ruta `/v1/portfolio/X` + degradación limpia en web); para estresar, REUSAR la maquinaria MC (no `calcular()`) y factorizar a una sola fuente pineada; el insight de diversificación temporal; la regla de consistencia greenfield; canonizar dimensiones antes de agrupar; y para una capa de SÍNTESIS/alertas, emitir señales estructuradas (motor) + copy en la web. **Pendiente:** redeploy del API (SHA `1d01f54`) para los endpoints `/v1/portfolio/{tesoreria,capital,tesoreria/estres,concentracion,salud}` en prod (la web degrada hasta entonces). **OJO datos prod:** prod tiene 3 proyectos (Argos/M8 no sincronizado a Supabase prod — se deja para después: "el negocio cambió mucho") → las cifras de portafolio en prod consolidan 3, no los 4 reales locales.
- **2026-06-16 — Pestaña "Fuentes y metodología" (procedencia de los datos macro del WACC) en 2 fases + la verificación viva DESTAPÓ un hallazgo de gobernanza (beta del sector) → memo al comité:** Martín pidió una pestaña para que el usuario sepa **de dónde salen** los datos macro (Damodaran, Banrep) del WACC. **Workflow de mapeo (3 lentes engine/api/web) → VERDAD CLAVE:** los macro del WACC están **HARDCODEADOS en cada JSON de proyecto** (`financiero.wacc`: rf, rm, rp, kd_us, betas, inflación…), **sin metadata de fuente** — la procedencia real solo vivía en un acta `.md` y en un `SourceNote` decorativo. Existe maquinaria M0/M6 (`engine/supuestos_macro.py`, `api/conectores/{damodaran,banrep,socrata}.py`, `macro_store.py`, `/v1/macro` con compuerta de aprobación) **pero DESCONECTADA del WACC** (solo cubre supuestos de política, no rf/rp/betas). **FASE 1 (PR #51, web-only, sin redeploy):** pestaña global `/fuentes` (sidebar) que muestra cada input macro **agrupado por fuente** (Damodaran, Banrep/DANE, FED/BLS, DIAN, CG) con su valor, uso en el build-up y **enlace**. Patrón honesto **sin drift ni hardcode**: la **fuente/método/enlace se curan** en `web/src/lib/fuentes.ts`, pero el **VALOR se lee EN VIVO del motor** vía el `getWacc` que ya existe (un proyecto representativo: el macro es común a todos) → cero cifras hardcodeadas en el front, ship inmediato. **FASE 2 (PR #52, toca api — NO el motor, dorado intacto):** contraste **modelo vs dato VIVO + desfase**. **Decisión de arquitectura clave: para MOSTRAR procedencia me salté toda la maquinaria M6 (Supabase + aprobación) — esa es para cuando el dato ALIMENTE el modelo (Fase 3, re-baseline).** En vez de eso, `api/fuentes_live.py` trae el dato vivo **cacheado por día** (`datetime.date.today()`; la tabla Damodaran es anual) y lo expone en `GET /v1/fuentes/live`; la web muestra "fuente hoy X% · Δ±Y pp" (ámbar) / "al día" (verde) y **degrada limpio a solo-modelo** si el API aún no tiene el endpoint (sin redeploy) o la fuente externa cae. **LOS CONECTORES FUNCIONAN EN VIVO** (los probé): Damodaran parsea su HTML (CRP Colombia 2.85%, ERP 7.08% → `pm` madura = ERP−CRP = 4.23%) y Banrep su SDMX (TRM/IBR). **Solo rp y pm mapean LIMPIO a inputs del WACC** (rp←CRP, pm←ERP madura); por eso Fase 2 cubre solo esos dos. **HALLAZGO DE GOBERNANZA (Opción A con Martín):** el modelo usa **`rp`=3.43% (BB−)** pero Damodaran HOY trae **2.85% (Baa3)** → desfase +0.58 pp visible (Colombia mejoró de calificación). Y al investigar la **beta del sector** para 2b saltó algo mayor: el modelo usa **`beta_us`=1.29** pero **HOY ningún sector construcción de Damodaran llega a 1.29** (Homebuilding 0.91 — y su D/E 21.34% calza con nuestro 21.56%, o sea ESE es el sector → la beta quedó rezagada o es de otra vigencia). **Cuantificado:** recalibrar a 0.91 bajaría el WACC 18.71%→**17.65% (−1.06 pp)**, lo que **mueve el Veredicto de Valor/EVA** (más proyectos "generan valor"). **POR QUÉ NO SE CONSTRUYÓ MÁS VIVO (lección de honestidad):** sumar betas/D-E al contraste **engañaría** sin confirmar el sector/año (¿comparas contra el sector equivocado?); inflación **no está en el SDMX de Banrep** (0 dataflows → necesita DANE) y es "esperada vs realizada"; impuesto tiene desajuste de atribución (DIAN vs Damodaran). **Mejor PARAR y llevar la pregunta al comité que pintar un "desfase" falso.** Se dejó `docs/hallazgo_beta_sector_20260616.md` (memo con tabla de impacto) para el comité; recalibrar la beta sería re-baseline → acta + aprobación. **Por qué importa:** queda el patrón "fuente curada + valor vivo del motor, sin hardcode" para cualquier vista de procedencia; la regla "mostrar vivo ≠ alimentar el modelo" (lo segundo toca motor + aprobación); y la lección de que **la verificación automática modelo-vs-fuente es un detector de drift de gobernanza** (descubrió la beta rezagada sola). **Pendiente:** redeploy del API (SHA `3524435`) para activar el dato vivo en prod; Fase 2b real (inflación DANE / rf Tesoro) solo si el comité lo prioriza.
- **2026-06-15 — Fase 2 de gráficas (pulido interactivo + 2 gráficas NUEVAS) sobre el sistema ECharts: patrones para gráficas DERIVADAS y para no romper "cero cálculo en el front":** Tras la re-piel (Fase 1), una **Fase 2** en 4 PRs, mismo flujo (rama→PR→Martín mergea, gate verde, solo `web/`, dorado intacto). **2A pulido (común a las 9 gráficas, sin tocar cifras):** (PR #44) animación de entrada `cubicOut` + **botón de descargar PNG** inyectados en el **wrapper `EChart`** (un solo archivo → toda gráfica lo hereda; `chart.getDataURL({pixelRatio:2, backgroundColor: tokens.tooltipBg})` exporta con fondo sólido del tema para pegar en presentaciones); (PR #45) **slider de zoom temporal** (`dataZoom` tipo slider, helper `timeDataZoom` en `echarts-timeline.ts`) en las 3 series mensuales largas (flujo/absorción/recaudo) — OJO: subir `grid.bottom` y la altura ~28px para que el slider NO choque con las etiquetas de año. **2B gráficas nuevas (las 2 que recomendó un workflow de descubrimiento):** **(PR #46) Cascada/waterfall del P&G** (pestaña Resumen) y **(PR #47) Comparador de proyectos** (dashboard). **PATRÓN CLAVE — "descubrir antes de construir una gráfica nueva":** un workflow (3 lectores) verificó qué expone HOY el API para cada candidata ANTES de codear → veredicto **cero bloqueadores**, y un matiz que se repite: **el dato suele YA viajar en el JSON pero NO estar tipado en `web/src/lib/api.ts`** (p.ej. `Results.distribucion`, `pyg.renta`); añadir el tipo es **web-only** (no toca motor/API). Sin este paso, arrancas un waterfall y descubres a mitad que falta una línea. **PATRÓN CLAVE — gráfica DERIVADA debe RECONCILIAR con un ancla del motor (mantiene honesto el "cero cálculo financiero en el front"):** el waterfall hace sumas/restas de PRESENTACIÓN, pero deben usar los **términos EXACTOS de la fórmula del motor** y **cerrar** en una cifra que el motor ya publica. Leí `modelo.pyg()` y la cadena correcta es `total_ingresos − costo_lote − directos − (indirectos_otros+gastos_fijos) − honorarios = util_oper` (NO `indirectos` a secas: se solaparía con `gastos_fijos`, porque `indirectos_otros = max(indirectos − gastos_fijos, 0)`); impuesto = `util_oper − udi`. Se añadió un **guard** (`console.warn` si la cascada no reconcilia >1 mil COP) → el propio gráfico es su test de fidelidad. Implementado con **`custom renderItem` (barras flotantes)** para manejar `util_oper` negativo de greenfield (el truco "placeholder transparente apilado" se rompe cruzando cero). **PATRÓN — comparador HONESTO en un tablero financiero:** ante métricas de unidades distintas (TIR/margen en %, VPN/ventas/valor en COP), se rechazó normalizar/radar (alturas relativas engañan) → **barras con VALOR y EJE reales, una métrica a la vez** (selector); respeta la constitución de cifras (`splitTir` omite TIR greenfield + nota; línea de cero para `valor_creado` → se ve cuándo un proyecto DESTRUYE valor, p.ej. Argos −$22,8 mil M bajo el cero). Reusa los `items` que el dashboard ya trae de `/v1/portfolio` (cero fetch nuevo; selección en estado React + `useIsDark`/`chartTokens` para colorear los chips con la misma paleta de las barras). **Por qué importa:** deja el wrapper como punto único para features transversales (PNG/anim), un helper de zoom temporal reusable, y dos patrones para CUALQUIER gráfica derivada futura (reconciliar contra el motor + descubrir el contrato antes de codear). **Decisión de alcance:** la **Curva S de costos** (3ª candidata, feasible: `Results.distribucion` ya viaja) se DIFIERE — su valor pleno depende de los `actuals`/SINCO (planeado vs real), que aún no existen; se retoma con la ficha de proyecto o cuando lleguen los actuals. **Verificado cada PR:** tsc + eslint + next build + gate `test` verdes; cada gráfica validada en claro/oscuro con preview ECharts por CDN (mismo molde de Fase 1).
- **2026-06-15 — Re-piel de TODAS las gráficas de /web de visx → Apache ECharts (Fase 1 COMPLETA, 6 PRs, dual-mode "vintage") + patrones reutilizables para cualquier gráfica nueva:** Se migraron las **7 gráficas visx** a ECharts (PR1 Flujo+infra · PR2 Mapa de valor · PR3 escenarios+tornado · PR4 heatmap+Monte Carlo · PR5 cronograma Gantt+absorción+recaudo · PR6 quita las 11 deps `@visx`), **cero cambios en engine/api**, **dorado intacto**, cada PR mergeada por Martín con gate verde. **Antes de tocar código: workflow de mapeo (9 lectores en paralelo) → veredicto CERO bloqueadores** (todo lo que pintan las gráficas ya lo expone el API; el front solo presenta) — patrón "entender antes de migrar" que evita el PARE por dato faltante. **INFRA reutilizable (en `web/src/lib` y `components/charts`):** `chart-tokens.ts` (paleta vintage anclada al teal CG, claro Y oscuro; semánticos éxito/alerta/peligro **solo** para estados) + `use-is-dark.ts` (hook `MutationObserver` sobre la clase `.dark` → re-pinta al togglear, SIN next-themes) + `echarts.ts` (instancia **tree-shaken**: solo los charts/components usados, no el bundle) + `echart.tsx` (wrapper: init/`setOption(buildOption(chartTokens(isDark)), true)`/`ResizeObserver`/dispose; **`backgroundColor:'transparent'` siempre** → el host pone el fondo). Regla de oro: `buildOption` memoizado con `useCallback` y **todos** los colores salen de `chartTokens(isDark)` (nunca `var(--…)` hardcodeado, que NO resuelve en canvas). **PATRONES/GOTCHAS clave (para la próxima gráfica):** (1) **`color-mix(in oklab,…)` NO se evalúa en canvas** → para replicar un mix con la tarjeta, pintar `rgba(color, alfa)` sobre el chart transparente (compone sobre el `--card` igual) — así migró el heatmap su `cellBg` y adapta solo a claro/oscuro. (2) **Ámbar de MARCA (`--cg-amber`) ≠ ámbar semántico (`alerta`)**: se añadió el token `cgAmber` (claro `#C8801F`/oscuro `#E3A340` = el oklch del CSS) para categoría/dirección (No-VIS, construcción, deuda, tornado-a-la-baja); usar `alerta` ahí viola la doctrina "ámbar/rojo solo para estados". (3) **Eje temporal**: usar `xAxis type:'value'` (NO `category`) para que el marcador **"Hoy" fraccional** (`mesesHastaHoy`) caiga exacto, y `axisLabel/splitLine.customValues` (ECharts ≥5.5; aquí 6.1) para marcas SOLO en enero de cada año. Helper compartido `echarts-timeline.ts` (`timeXAxis`+`hoyMarkLine`). (4) **Histograma y Gantt → `custom series` con `renderItem`** (rects exactos por bin / 2 pistas + anillo PE): el `bar` sobre eje `value` no da el control de ancho/posición que sí da `renderItem` (`api.coord`/`api.size`). (5) **`symbolSize` de ECharts es DIÁMETRO** (visx daba radio) → ×2 en el scatter. (6) **`showMinLabel/showMaxLabel:false`** limpia las etiquetas de borde "cramped" cuando fijas `min/max` exactos. (7) **`markLine` por defecto trae label** → `label:{show:false}` salvo el que quieras (la línea cero, exposición, etc.). **VERIFICACIÓN visual SIN el bug de las screenshots en blanco:** la app viva renderiza post-hidratación y agent-browser captura el canvas en blanco → patrón que funciona: **preview HTML autónomo con ECharts por CDN** que replica el `buildOption` con datos sintéticos realistas, en dos paneles claro/oscuro → screenshot nítido (validé las 7 así, ajustando detalles antes de commitear). **OJO con los datos sintéticos**: si quedan fuera de rango (hurdle/markers fuera del min/max, recaudo demasiado chico para el eje `/1e6`) el preview "miente" — escalar a magnitudes reales. **JUICIO de alcance (no migrar por dogma):** el **heatmap** (grilla de datos con valores/ejes/tooltip) SÍ se migró a ECharts `heatmap` (color por celda vía `itemStyle.color` por ítem, **sin `visualMap`** — funciona); la **barra de composición del WACC** (proporción de 10px, 2 segmentos, etiquetas de texto) se **dejó en CSS** a propósito: no es una gráfica, ya es dual-mode y el canvas le quitaría el `tabular-nums` sin ganar nada. **Reglas que NO se propagan:** el **filtro greenfield / "jamás TIR −99%"** aplica SOLO al Mapa de valor (doble salvaguarda: el API ya manda `tir_apalancada_ref or tir_proyecto` + el front filtra `> TIR_DEGENERADA`); NO meterlo en Monte Carlo (valores crudos intencionales) ni en gráficas de util_oper/recaudo/unidades. **Por qué importa:** queda un sistema de gráficas ECharts dual-mode con tokens centralizados + helpers (tiempo, rgba, cgAmber) y un molde de verificación (preview CDN) para CUALQUIER gráfica futura; y la lección de criterio: migrar lo que es gráfica, dejar en CSS lo que es UI. **Pendiente menor:** si Martín quiere la barra WACC en ECharts por consistencia, es trivial pasarla a barra apilada.
- **2026-06-15 — Veredicto de Valor (EVA del proyecto) añadido — métrica ADITIVA, dorado intacto + 3 patrones reutilizables (métrica aditiva sin re-baseline, "periodicidad vs serie", web que degrada si el API está atrás):** Se añadió el **"¿genera o destruye valor sobre el WACC?"** (binario de comité). **Engine** (`valor.py` puro + inyección en `apalancamiento.py`): `crea_valor`=TIR proyecto>WACC (ambas anuales), `spread_valor`=TIR−WACC (pp), `valor_creado`=VPN del flujo del proyecto **@WACC** (espejo del VPN@TIO pero al WACC). **API** (`build.py`): en `indicadores` + portafolio (item + consolidado Σ valor_creado, excluye greenfield). **Web**: `ValorBanner` (GENERA verde/DESTRUYE rojo/greenfield neutro). **NUANCE CLAVE (corrige un aprendizaje viejo):** el `flujo.vpn_proyecto` secundario que salía **muy negativo** NO era periodicidad — `modelo.flujo_caja` YA usa `wacc_m=(1+wacc)^(1/12)−1`. La causa real: descuenta el `flujo` **crudo** (sin los reintegros de honorarios+lote), serie distinta a la de DECISIÓN. `valor_creado` usa la MISMA serie del VPN@TIO (`retorno`/FCL) → sale **positivo** cuando TIR>WACC (Navarra **+13,9 mil M**). Lección: "VPN negativo con TIR>WACC" casi nunca es la tasa; es la **SERIE**. **PATRÓN MÉTRICA ADITIVA (sin re-baseline):** un campo nuevo en el `result` de `calcular()` ROMPE el harness dorado (compara `set(esperado)|set(actual)` → el campo nuevo es "desviación") → hay que **regenerar los snapshots** y verificar por **`git diff` que SOLO aparecen los campos nuevos** (TIR/VPN@TIO/flujo INTACTOS). Si el diff muestra solo adiciones → es aditivo, **no requiere acta** ni aprobación (no mueve cifras de decisión). **GOTCHA web-adelante-del-API:** la web auto-despliega en Vercel pero el **API se redeploya a mano** → en la ventana, un campo nuevo (`crea_valor`) llega **`undefined`** y `undefined ? 'genera' : 'destruye'` pintaba un **falso DESTRUYE**. Fix: `== null` (cubre undefined→neutro) + **renderizar el badge solo si el API ya trae el dato** (`valor_metodo`) → la web degrada limpio yendo adelante del API. **VALOR DEMOSTRADO:** la métrica detectó que **Argos DESTRUYE valor** (TIR 7,9% < WACC 18,71%, valor_creado −22,8 mil M), algo que el margen/VPN@TIO no gritaban; portafolio neto +3,5 mil M (2/3 generan). **Greenfield** (TIR degenerada <−0,5/None) → veredicto None ("— greenfield", jamás falso "destruye"). Verificado: engine **101** / api **66** verdes, **dorado EXACTO** (TIR 37,60% / VPN@TIO 18,28 mil M / TIR socio 41,72%). Commits por módulo (PRs #33, #34). **Pendiente:** redeploy del API para el veredicto en prod (la web ya está, degradada hasta entonces).
- **2026-06-15 — Streamlit RETIRADO — prototipo cumplió su ciclo; stack productivo = FastAPI + Next.js. NO fue un borrado en bloque: `app_streamlit/` era el hogar del DORADO + datos + secretos (extraer ANTES de borrar):** Se retiró la app Streamlit. **El audit (workflow 4 lentes) destapó que `app_streamlit/` NO era solo UI:** alojaba (a) los **snapshots dorados** (`tests/golden/`, que `engine/tests/_golden.py` LEE — borrarlos dejaba el harness con **0 snapshots → verde FALSO**, el dorado destruido en silencio), (b) los **JSON de proyectos** reales+ilustrativos (los lee `api/repo.py` fallback, el ETL, `refresh_scenarios.py`), (c) los **secretos Supabase** (`.streamlit/secrets.toml`), (d) **3 piezas ÚNICAS** sin copia en engine: `navarra_data.py` (datos operativos/EVM reales de Navarra — avance 42% comité, presupuesto T1, alertas), `snapshot_dorado.py` (regenerador del dorado), `test_anclas.py` (anclas auditadas del comité). **Borrar en bloque (como se pidió) habría roto el dorado + la API + el ETL.** Plan seguro ejecutado: **EXTRAER → RECABLEAR → VERIFICAR → BORRAR**. Reubicación: golden→`engine/tests/golden/`, datos→`data/proyectos{,_privados}/`, regenerador→`engine/execution/`, tests únicos→`engine/tests/`, secretos→`/.streamlit/secrets.toml`, `navarra_data.py`→`data/` (preservado para el EVM futuro). Recableados ~11 archivos (`_golden.py`, `repo.py`, `refresh_scenarios.py`, `etl_import_v1.py`, `test_{anclas,finanzas,schema,vehiculos,goal_seek,simulacion}`, `snapshot_dorado.py`). **`.gitignore` raíz endurecido ANTES de mover** (datos REALES + secretos nunca al repo). Borrado: `app_streamlit/`, `Dockerfile.streamlit`, `deploy_streamlit.*`, `dev.*`; `test.ps1/sh` y `ci.yml` ahora corren **engine + api** (sin paso app_streamlit). **Verificado: engine 94 / api 65 verdes; el harness dorado encuentra los 7 snapshots y pasa (no skip)** — la red de seguridad sobrevivió la mudanza. **EXCEPCIÓN a "paridad antes de apagar":** el Monitor/EVM no tiene equivalente en /web (espera los `actuals` de SINCO); se retiró **preservando su dato** (`data/navarra_data.py`), no la vista. **Infra Azure pendiente (Martin):** detener el App Service `cg-factibilidad-app` (RG `Cg-factibilidad`); **NO borrar el ACR `cgfactibilidadacr`** (compartido con el API `alephapi`), solo opcional limpiar tags `cgapp:*`. **Por qué importa:** una carpeta "deprecada" puede ser load-bearing — el audit antes de borrar es obligatorio; y un harness que lee fixtures de otra carpeta puede pasar en VERDE FALSO si la carpeta desaparece (0 casos = 0 fallos). Acta/runbook del retiro en el PR.
- **2026-06-15 — Sincronizar un cambio de DATO a prod NO es solo redeploy: prod lee `scenarios.snapshot` CONGELADO + el API recalcula EN VIVO (orden redeploy→refresco para no quedar Frankenstein):** Al ir a reflejar el WACC 18,71% en prod se destapó un patrón clave de la arquitectura. **(1)** Prod lee el `par` de cada proyecto desde **`scenarios.snapshot` (Supabase, modelo objetivo)** — NO de los JSON del repo (esos son solo local/CI). Ese snapshot lo congeló el **ETL del 12-jun (Fase 4b), antes de M1**, y es **INMUTABLE** (trigger 0002). **(2)** El API **recalcula en vivo** (`build.cargar_calcular` hace `calcular(par)` por request; **NO** sirve `results_cache`). **Consecuencias:** un cambio que vive en el DATO (inputs del WACC `rf/rm/rp/kd_us`) **no llega a prod con un redeploy** — hay que **refrescar el dato** (como el snapshot aprobado es inmutable, se **crea una versión nueva aprobada v2**; `repo._snapshot_de_scenario` lee la versión más alta → prod pasa a leerla). Y un cambio que vive en el CÓDIGO (fix flujo_equity de PR#22 → `tir_equity`; exención VIS M2 → `udi`; vehículos) **sí** necesita redeploy. **GOTCHA contraintuitivo:** el WACC 18,71% **sí** sale con el refresco SOLO (sin redeploy), porque `kd_us=5,9` y `rp=3,43` van **explícitos** en el par v2 y `finanzas.calcular_wacc` los lee del dato (la fórmula no cambió). PERO si refrescas el dato sin redeployar, el motor VIEJO recalcula `tir_equity`/VIS con código pre-M1-M8 → **estado Frankenstein** (WACC bien, cifras de decisión viejas), **invisible al gate local** (que corre con el motor nuevo). **Por eso el ORDEN es REDEPLOY primero, REFRESCO después** (así nunca hay cifras de decisión viejas; el WACC viejo intermedio es solo exhibición). **Herramienta nueva:** `db/refresh_scenarios.py` (DRY-RUN por defecto, `--apply` para escribir, `--check-only` sin Supabase) — crea el scenario v2 approved con el par actual, con **gate dorado que ABORTA antes de escribir** si las cifras no reproducen el golden (+ aborta si falta el golden en `--apply`), idempotente (skip por hash), inmutabilidad respetada (INSERT, no UPDATE). Runbook completo: `docs/runbook_sync_wacc_prod.md`. **Verificado adversarialmente** (workflow 4 agentes: una lente marcó un falso positivo de idempotencia en la línea del hash que el revisor jefe REFUTÓ — `_hash` ya normaliza con `_con_schema`+`sort_keys` ambos lados; no se aplicó). **El MCP de Supabase de Claude sigue SIN alcanzar la BD de la app** (`jehkdhmngxvuvhxuhlan`; solo ve otra org) → el refresco lo corre Martín local con la `service_role`, como el ETL. **Por qué importa:** para cualquier cambio futuro que toque cifras de proyectos en prod, distinguir DATO (→ refrescar scenarios) vs CÓDIGO (→ redeploy), y respetar el orden; vale para M1-M8 entero (prod está pre-M1-M8 hasta correr esto).
- **2026-06-14 — WACC dejado defendible (beta_d BBB + rp Colombia BB-) → 18,71%; APROBADO por Martín + dónde va el riesgo país:** Se cerró el afinamiento que M1 dejó pendiente: la **beta de la deuda** salía **1,19** (indefendible — implica deuda más riesgosa que el mercado entero). Causa: `beta_d = (kd_us − rf)/prima_mercado` es la fórmula de Damodaran (*spread default ÷ ERP*), y tras M1 (rf 0,12→4,30; prima 12,32→4,23%) el `kd_us` heredado de 9,335% dejó un spread implícito de 5,04% = calificación CCC. **GUÍA EXPERTA (Martín preguntó "Colombia es BB-"):** el riesgo **SOBERANO** de Colombia (S&P bajó a **BB- el 8-abr-2026**, FC) va en **`rp`** (CRP, se suma al Ke), **NO** en `beta_d` — meterlo en `beta_d` lo **doble-cuenta** (el error que M1 auditó). `beta_d` es el riesgo de mercado/negocio de la deuda del **comparable** (homebuilder US, D/E 21,6% = grado de inversión **BBB**). **Fix (2 correcciones, cada riesgo en su canal):** `kd_us` 9,335→**5,90%** (spread BBB 1,6% → `beta_d` **0,38**) en los 6 JSON + default de `finanzas.calcular_wacc`; `rp` 2,85→**3,43%** (spread BB- de Colombia, misma convención con que M1 fijó BB→2,85% — **[VALIDAR]** con la fuente Damodaran del comité, rango BB- 3,3–3,6%). **MECÁNICA CONTRAINTUITIVA:** bajar `beta_d` **SUBE** el WACC (con D/E 233%, una deuda más "segura" empuja riesgo al equity → `beta_l` 1,41→**2,35**, `beta_u` 1,27→**1,15** ≈ sector Damodaran). **WACC 17,31% → 18,71%** (el 17,31% estaba subestimado). **DORADO INTACTO** (verificado por `git diff` estricto: solo se mueven `wacc`, `rp`, `kd_us` y el VPN secundario @WACC): TIR proyecto **37,60%**, VPN@TIO **18,28 mil M**, TIR socio **41,72%**, `tir_equity`, flujo, crédito — idénticos (el WACC es **exhibición**; las decisiones se descuentan @TIO 15%). **Colateral:** `app_streamlit/tests/test_anclas.py` tenía `ap_tir_equity` VIEJOS (PR #22 flujo_equity regeneró snapshots pero no sincronizó esta copia → suite app rojo en `main` desde entonces); se sincronizó a los snapshots commiteados (NO es cifra nueva, ya estaba aprobada en PR #22). **Verificado:** engine **66** / api **65** / app **75** verdes. Acta: `docs/acta_rebaseline_wacc2_betad_rp_20260614.md`. **Por qué importa:** patrón "cada riesgo en su canal" (soberano→`rp`, negocio→`beta`) evita el doble conteo; y queda claro que en este build-up apalancado **subir la calidad crediticia de la deuda SUBE el WACC**. **Follow-ups (no en este PR):** `app.py:252` (template "nuevo proyecto" aún con WACC pre-M1 `rf:0.12`; app congelada, proyectos nuevos van por `/web`); confirmar `rp` con fuente del comité; `flujo.vpn_proyecto` secundario sale muy negativo pese a TIR>WACC (posible periodicidad mensual vs tasa anual — revisar aparte); **redeploy del API** para reflejar 18,71% en prod.
- **2026-06-14 — Spec P&G dinámico COMPLETA (M1–M8) aterrizada vía Cowork (aprobada por Martín) + re-baseline del dorado + corrección del PROCESO multi-agente (Regla #6):** Una sesión de **Cowork** implementó **toda la `spec_pyg_dinamico.md` en ~21 commits directo a `main`**: **M1** (recalibración WACC), **M2** (exención renta VIS en el P&G), **M3** (vehículos jurídicos + waterfall after-tax + comparador), **M4/M4b** (goal-seek + simulador en vivo con sliders), **M5** (Monte Carlo Crystal Ball: distribuciones/percentiles/certeza/tornado), **M6.5** (persistencia de supuestos_macro + cron-refresh con compuerta), **M7** (rediseño UX del lienzo liderado por el P&G), **M8** (Argos, 4º proyecto). **Re-baseline del dorado APROBADO por Martín** (lo confirmó explícitamente), con **2 actas** (`docs/acta_rebaseline_wacc_20260614.md`, `docs/acta_correccion_vis_20260614.md`): **WACC 21.54% → 17.31%** (recalibración Damodaran — `rf` 0.12%→4.30% estaba absurdo; solo mueve el WACC + el VPN@WACC secundario) y **exención de renta VIS** en la línea UDI (solo `pyg.renta`/`udi` de proyectos VIS; Navarra UDI 22.520→26.497). **CLAVE — los anclajes CORE del dorado NO se tocaron:** TIR proyecto **37.60%**, TIR socio **41.72%**, **VPN@TIO**, flujo, crédito, recaudo, ventas — **idénticos** (las decisiones se descuentan a la TIO, no al WACC; TIR/VPN son antes de impuestos). **El "WACC 21.54%" de aprendizajes viejos queda SUPERSEDED por 17.31%.** Verificado en esta sesión: **engine + api 65 + web build verdes** (`main` sano). **LECCIÓN DE PROCESO (el "error" que Martín pidió corregir):** varios agentes (VS Code + Cowork) commitearon a `main` SIN ramas → trabajo sin commitear que se pisa, `index.lock` colgado, 21 commits sin PR. **El trabajo es bueno y está aprobado → SE QUEDA** (decisión con Martín: "ordenar el proceso", no revertir). Se corrigió la GOBERNANZA con la **Regla innegociable #6 (disciplina multi-agente)**: rama por módulo/agente, nada directo a `main` con varios agentes, gate dorado + **esperar la aprobación de Martín ANTES de tocar el dorado** (y NUNCA escribir "aprobada" en un acta sin que lo esté), commit+push por módulo, fetch antes de arrancar. **Por qué importa:** sin disciplina de ramas, la concurrencia multi-agente es un riesgo real de pérdida/pisón de trabajo; con la Regla #6 + el gate de aprobación, se previene. **Pendiente:** (1) **redeploy del API** (SHA reciente) para activar M1–M8 en prod; (2) revisar los **`[VALIDAR]` tributarios** (exención VIS, vehículos) con el asesor antes de llevar las cifras de DECISIÓN a after-tax (Layer B, decisión de Comité); (3) montar la disciplina de ramas para el trabajo que siga.
- **2026-06-14 — Committeados M0 (Supuestos Macro) + M6 (conectores) de la spec P&G dinámico — trabajo de OTRA sesión + gotcha de stale lock multi-sesión:** Apareció en el working tree trabajo NO hecho por esta sesión: **M0** (`engine/aleph_engine/supuestos_macro.py` + `Metric` con `estado_validacion`/`fuente_normativa` + `db/migrations/0003`) y **M6** (`api/aleph_api/conectores/` Banrep/Socrata/Damodaran + tests), bajo una spec nueva **`directives/spec_pyg_dinamico.md`** (módulos M0–M8: P&G dinámico, tributación VIS, costo de capital, vehículos, goal-seek, Monte Carlo Crystal Ball). **Cómo se committeó trabajo ajeno con responsabilidad:** (1) **VERIFICAR ANTES** — `engine` 38 verdes + **dorado INTACTO** (el cambio de `metrics.py` es **ADITIVO**: 2 campos nuevos al dataclass, no toca ninguna cifra; M0/M6 son "no mueve cifras" por diseño), `api` 56 verdes; (2) **commit POR MÓDULO** (M0 `dfd7afe`, M6 `f26cc68` separados) como pide la spec, no un blob; (3) **NO fabricar el aprendizaje sustantivo** del módulo (lo añade quien lo construyó; aquí solo se registra el hecho + la verificación). **GOTCHA stale lock:** la otra sesión dejó un **`.git/index.lock` colgado** (sesión que termina/crashea sin liberarlo) → git daba `fatal: Unable to create index.lock`. Es **distinto** del gotcha de OneDrive ya archivado (aquí el repo NO está en OneDrive): vino de **otra sesión**. **Cómo resolver seguro:** confirmar que **NO hay proceso git activo** (`Get-Process git,git-remote-https`) + que el lock es viejo/vacío (LastWriteTime + Length 0) → recién ahí `Remove-Item .git\index.lock -Force`. **Por qué importa:** este repo lo tocan **varias sesiones/agentes** → puede haber trabajo sin commitear + locks colgados; el orquestador verifica (verde + dorado) y commitea por módulo. **GOTCHA del tool PowerShell:** un comando que combinaba `git commit` (here-string) + `git push | Select-Object` + `git status` disparó un guard del sandbox ("Remove-Item on system path '/' blocked") y NO corrió → separar `commit` y `push` en llamadas distintas lo evitó. **Pendiente:** M0/M6 NO requieren redeploy urgente (M0 aditivo, M6 conectores aún no cableados al flujo vivo); la próxima sesión sigue la spec (M1 costo de capital o M2 tributario mueven cifras → exigen re-baseline dorado + acta + aprobación de Martín).
- **2026-06-13 — CRUD COMPLETO (admin Inc.2: editar + marcar real) + LECCIÓN META: un harness puede dar FALSO NEGATIVO si el fixture enmascara el bug:** Cerrado el CRUD: el `/web` ya **edita** proyectos y los marca **reales/ilustrativos**. **Editar** = botón en la ficha → formulario **pre-llenado** (mismo `ProyectoForm` reusado, `parseParToForm` invierte `buildPar`) → guardar crea una **versión nueva** (escenario v(n+1)) y la aprueba; la anterior queda **inmutable** (trigger 0002). **Marcar real** = `PATCH /v1/projects/{slug}` (flag de proyecto, no toca cifras). API: `GET …/source` (par crudo, admin-only) + `PATCH …` + `write.obtener_para_editar/marcar_real` (api **25** verde). **LO IMPORTANTE — la verificación adversarial salvó el día:** mi harness determinista dijo "editar-sin-cambios preserva la TIR (diff 1.6e-12) ✓" pero era un **FALSO NEGATIVO**: pasó SOLO porque Navarra trae un override `fiducia` (fcl_proyecto/fcl_socio_cg hardcodeados) que hace al flujo apalancado **saltarse los hitos recalculados** → enmascaró un cronograma corrupto. El **workflow adversarial** (que probó contra un proyecto SIN fiducia) destapó **bugs CRÍTICOS** que habrían corrompido cifras reales: **(1) CRÍTICO `sucesora`** — `buildPar` reescribía `sucesora` a cadena SERIAL (`i==0?null:i`), pero el formulario es un modelo **DATADO** (cada etapa arranca en su `fecha_inicio`); el serial re-anclaba las etapas 2..N a la PE de la anterior e IGNORABA su fecha → en un proyecto no-fiducia el **VPN cambiaba de signo** en un "editar sin cambios". Fix: **`sucesora=null` para todas**. **(2) ALTO tipologías** — para proyectos con `tipologias` (Navarra), `normalizar_tipologias` re-deriva und/precio/ventas del bloque de tipologías → editar esos campos se **descartaba en silencio**. Fix: **bloquear la edición de proyectos con tipologías** (aviso honesto; edición de tipologías = fase futura). **(3) ALTO `ventas_vivienda/adicional_miles` viejos** sobrevivían el overlay y corrompían el recaudo → fix: **borrarlos** en el overlay (el motor cae a `ventas_miles`). **(4) MEDIO overlay posicional** barajaba los extras al borrar una etapa intermedia → fix: **overlay por IDENTIDAD** (`_orig` en cada fila). **(5) ALTO baseline invisible** — la lectura priorizaba `baseline` sobre la versión mayor → una edición quedaba invisible si había baseline (hoy latente: nadie usa baseline) → fix: **leer la versión MÁS ALTA** (`repo._snapshot_de_scenario` + `obtener_para_editar`). **Re-verificado con fixture DATADO no-fiducia:** el fix es **no-op** (ΔVPN 0.0007%, margen/TIR exactos) y el bug habría corrompido **12%** (ΔVPN 855.796). El **dorado nunca se toca** (editar crea versión nueva; la v1 dorada es inmutable). **REGLA para verificar:** un fixture con propiedades especiales (override de fiducia, datos hardcodeados) puede **saltarse el code path bajo prueba** y dar verde falso → verificar SIEMPRE contra un caso SIN la propiedad enmascarante; y el **harness que replica** el código no ve bugs del código (por eso el workflow que LEE el código real es complementario, no redundante). `next build` verde. **PENDIENTE:** redeploy del API (endpoints nuevos source/patch); edición de tipologías + atomicidad fuerte (RPC) en fases futuras.
- **2026-06-13 — Capa de ADMINISTRACIÓN Inc.1 (role-gating + eliminar proyecto) + gotcha de NextAuth v5 (token filtrado al cliente):** Tras validar Fase 5 en prod, se arrancó la **capa de admin** (para ajustar/quitar proyectos — Martin creó "Montevedra" con datos de ejemplo y hay que poder borrarlo). **Inc.1 construido:** (a) **rol en la sesión `/web`** — `auth.ts` decodifica el claim `roles` del access token (callback jwt→session) y `lib/session.ts` `isAdminUser()` (server-only; dev=true porque el API local está abierto); (b) **gating de UI** — el botón "Nuevo proyecto" y la "Zona de administración" de la ficha solo se renderizan para admins; (c) **`DELETE /v1/projects/{slug}`** (`require_admin`) → `write.eliminar_proyecto` audita ANTES + borra results_cache→scenarios→projects (orden FK-safe, resuelve slug→id); (d) **UI de borrado con confirmación escribiendo el nombre exacto** (in-place, sin modal — la doctrina de diseño prohíbe modal-first). **VERIFICACIÓN ADVERSARIAL (workflow de 3 lentes — patrón build→verify):** el **gate es REAL, no cosmético** — un usuario "gerencia" (autenticado sin rol admin) NO puede borrar ni invocando la Server Action directo (adjunta SU PROPIO token → API 403); el gating de UI (`isAdminUser`/`session.isAdmin`) es **puramente cosmético** (decodifica el JWT sin verificar firma) y NUNCA decide autorización (el API revalida firma+aud+iss+tid+exp y exige `is_admin`). **GOTCHA REAL hallado (MEDIUM, pre-existente):** en **NextAuth v5 el objeto que devuelve el callback `session()` es EXACTAMENTE lo que `GET /api/auth/session` entrega al NAVEGADOR** → `session.apiToken` (el access token de Entra, Bearer del API) era **leíble por cualquier JS del cliente** (XSS/extensión → exfiltración; es el token del propio usuario, no escala, por eso MEDIUM). **Fix sin tocar el path crítico:** se envuelve el `GET` en `app/api/auth/[...nextauth]/route.ts` para **quitar `apiToken`/`apiTokenExp` de la respuesta HTTP de `/session`** — `auth()` server-side decodifica la cookie directamente (NO pasa por esa ruta) así que `api.ts` sigue adjuntando el Bearer sin cambios; **nada del app usa `/api/auth/session`** (no hay `useSession`) → blast radius nulo. **Regla:** nunca poner un bearer/secreto en el objeto `session` (va al cliente); el access token vive solo en el JWT cifrado + se lee server-side. **GOTCHA dev:** `/api/auth/session` da 500 `MissingSecret` en local (auth apagada, sin `AUTH_SECRET`) — pre-existente, irrelevante (dev no usa auth). api **24 verdes** (+2 tests del delete) · `next build` verde · render+confirmación verificados. **Inc.1 VALIDADO EN PROD (2026-06-13):** redeploy del API hecho (SHA `d5001eb`; verificación independiente: `DELETE /v1/projects/__probe__` sin token = **401** → ruta nueva viva) + Martin **borró Montevedra desde la UI** (ficha→Zona de administración→confirmar nombre) → `/health/data` `project_count` 3→4→**3**. El ciclo **crear→borrar funciona end-to-end** con auth Entra real + gating de admin. (Gotcha de verificación: "no aparece project_count 3" era correcto = 4 incluía a Montevedra; al borrarlo volvió a 3.) **PENDIENTE — Inc.2: editar proyecto** (reabrir borrador→form pre-llenado→re-aprobar nueva versión) + **marcar `es_real`** (hoy el form crea todo como "ilustrativo").
- **2026-06-13 — Verificación adversarial de Fase 5 destapó un bug de constitución PRE-EXISTENTE (TIR −99% en pantalla) + lecciones de "probar el flujo":** Antes de que Martin probara el alta en prod, verifiqué el flujo **ejecutando yo el camino crítico**: (1) **harness Python que replica fiel `buildPar`** del formulario → `schema.parse` + `calcular` + `checks` sobre varios proyectos; (2) **workflow adversarial de 3 lentes** (UI −100%, buildPar-vs-motor, write-path). Hallazgos clave: **(A) BUG REAL de constitución, ya en prod:** la ficha (`ficha-resumen.tsx`) pintaba `tir_proyecto` **crudo** con `splitPct` → **Torres de Campiñas SIEMPRE mostró "−99.40%"** como KPI-héroe (viola "jamás TIR −99%"); ni motor, ni API, ni `format.ts` aplicaban la regla greenfield. **Fix (web, sin tocar motor):** helper compartido `splitTir`/`tirEsDegenerada`/`TIR_DEGENERADA=-0.5` en `format.ts` → TIR degenerada (null o <−0.5) se muestra **"— greenfield"**, aplicado a ficha (TIR proyecto **y** socio), `portfolio-table`, `pipeline`, y el filtro de `value-map`. **VERIFICADO en Torres real:** la ficha ahora dice "— greenfield", `−99/−100` no aparece en ningún lado. **(B) Lección de "probar el flujo":** los **valores de ejemplo deben ser realistas** — un lote al 38% de ventas (mi primer ejemplo) da margen **−24%** (proyecto que pierde plata); el lote sano es ~7-8% de ventas (como Navarra). Y **algunos proyectos de 1 etapa dan `tir_proyecto` degenerada (−100%) AUNQUE tengan VPN y margen positivos** (IRR sin cruce de cero) → por eso el fix (A) es imprescindible; un ejemplo multi-etapa o `$/und` da TIR proyecto sana (25-33%). **(C) buildPar verificado campo-por-campo vs motor: SIN desajustes** (todas las claves de indexación directa seteadas, escalas correctas: `pe_pct/100`, `costos/100`, `ventas und*precio*area/1000` == `modelo._ventas_tipologia`); único fix: **redondear a entero** `und`/`vmes`/`dur_obra`/`escrituracion` (`numInt`) porque el contrato los tipa `int` y un decimal da **422 técnico de Pydantic**. **(D) Endurecimiento del API** (`write.py`, requiere **redeploy** — el form no lo dispara porque siempre manda datos completos): `aprobar` envuelve `calcular()` → **422 legible** en vez de **500 opaco** (un par sin `financiero.wacc` PASA `schema.parse` pero CRASHEA el motor por API directo); **delete compensatorio** si el insert de scenario/audit falla tras crear el proyecto (no deja proyecto-sin-escenario bloqueando el slug); mensaje de slug-dup más claro. **Diferido:** guard TOCTOU de `aprobar` (LOW, incompatible con el fake-SB de tests que devuelve `[]` en updates), atomicidad fuerte vía RPC (Fase 3). **META-LECCIÓN:** un harness que **replica** `buildPar` NO puede ver bugs que estén DENTRO de `buildPar` ni en la **capa de presentación** — el workflow leyendo el **código real** atrapó ambos (el bug de display y la revisión de buildPar). **Harness determinista + workflow adversarial = complementarios**, no redundantes. api **+1 test** (aprobar par incalculable → 422), web `next build` verde.
- **2026-06-13 — Fase 5 (ESCRITURA) arrancada: el `/web` ya CREA proyectos (no solo lee) — bridge + formulario + gotchas:** El `/web` pasa de solo-lectura a **lectura+escritura**: hay un formulario `/proyectos/nuevo` (entrada desde el botón "Nuevo proyecto" del dashboard) que arma el `par`, lo manda al API (crear→aprobar), el **motor recalcula** y el proyecto queda visible. **Bridge `/web`→API (`api.ts`):** `apiWrite` adjunta el Bearer y **distingue 403-no-admin (mensaje claro, NO redirige — ese usuario SÍ está autenticado) de 401-expirado (sí redirige a /login)**; superficie el `detail` del API (incluido el **422 de Pydantic, que llega como LISTA** `loc:msg` → texto legible). Server Action `crearYAprobarProyecto` (`lib/actions.ts`, `"use server"`) hace crear→aprobar en **2 pasos**: si **aprobar** falla, el borrador YA quedó guardado → el mensaje lo dice (evita reintentar con el mismo nombre y chocar con **409 slug duplicado**); el `redirect()` de 401 se deja **propagar** (no atrapar, es control-flow del framework). **GOTCHA del motor (clave):** cada etapa necesita `ventas_miles` **EXPLÍCITO** — `modelo.py` lee `e["ventas_miles"]` directo (solo lo deriva vía `tipologias`) → `buildPar` lo calcula `und×precio×area_und/1000` (`$/m²`, fórmula de `modelo._ventas_tipologia`) o `und×precio/1000` (`$/und`). El usuario llena **solo lo específico** (datos, etapas, costos %, lote); **WACC build-up + financieros + cronograma van con el ESTÁNDAR CG pre-cargado** (plantilla Navarra), editables por proyecto después. **Decisión técnica:** formulario **controlado con `useState`** (sin react-hook-form/zod) — minimizar piezas porque **la escritura SOLO se prueba en prod** (en local el API da 503 sin Supabase); el **gate real es `schema.parse` del API**, el cliente solo hace checks de UX. **GOTCHA dev server (Next 16):** un `next dev` viejo (de **antes** de crear los archivos) sirve un estado **RANCIO** — HTTP 200 pero sin el contenido nuevo y **sin CSS** (body en Times New Roman, `main` vacío) → **matar por puerto** (`Get-NetTCPConnection -LocalPort 3000 | Stop-Process`) y relanzar; verificar con `Invoke-WebRequest` (marcadores del form en el HTML) + **DOM eval** (`inputs.length`, `bodyFont`), NO fiarse del screenshot de agent-browser (artefacto de captura ya conocido). **GOTCHA NUEVO (2026-06-19) — `next dev` NO hidrata en agent-browser:** al verificar UI INTERACTIVA (tabs, toggle de tema, `useEffect`/contexto cliente) en `next dev`, los componentes cliente **no hidratan** en la sesión de agent-browser (los tabs no cambian, el tema no togglea, los efectos no corren) **aunque `tsc`+`eslint`+`next build` estén verdes**; y un `.next` rancio NO es la causa (pasa con dev recién levantado + `.next` borrado). **La causa es el modo dev en esa sesión, no el código → verificar UI interactiva/hidratación en PRODUCCIÓN (`next build` + `next start`), NO en dev** — ahí hidrata bien y el render claro/oscuro es fiel (validado en el header permanente de la ficha y en el Monte Carlo Crystal Ball). **Sub-gotcha:** leer estado React justo después de un `.click()` por eval da el valor VIEJO (los updates son async) → patrón: click → esperar ~1.5s → leer en un eval SEPARADO. **Por qué importa:** queda el **patrón reutilizable de escritura** (apiWrite + Server Action de 2 pasos + formulario controlado) para todas las pantallas de Ingreso siguientes; `next build` verde + render verificado (light **y** dark, ambos impecables). **Fase 5 VALIDADA EN PROD (2026-06-13):** Martin (logueado como admin) creó+aprobó un proyecto de prueba de 2 etapas (TIR proyecto ~25.5%) desde `/proyectos/nuevo` → funcionó a la primera. El camino de escritura completo (form → Server Action → API con auth Entra real → `schema.parse` → `calcular` → Supabase → ficha) está VIVO en producción: el `/web` ya CREA proyectos, no solo lee. Habilitador real para apagar Streamlit. **Pendiente Fase 5:** (1) **role-gating de UI** (hoy el botón lo ven todos; un no-admin recibe 403 claro — falta ocultarlo a gerencia leyendo el rol de la sesión); (2) edición de **borradores** + WACC/financiero editables por proyecto; (3) **redeploy del API** para activar el hardening de `write.py` (opcional, el form no lo dispara); (4) Fase 4 (RLS granular).
- **2026-06-13 — Camino de ESCRITURA: diseño profesional (directiva) + Fase 1 (cut-over de lectura a scenarios) HECHA:** Tras cerrar la paridad de lectura, se diseñó el **camino de escritura** (de `/web` solo-lectura a crear/editar proyectos = el habilitador para apagar Streamlit) con un **workflow de 7 agentes** (2 mapearon, 5 diseñaron: datos, API, seguridad, UX, roadmap). El diseño quedó en **`directives/plan_escritura.md`** (Capa 1): modelo `projects→scenarios` (draft/approved/baseline, snapshot JSONB **inmutable por trigger**, un baseline/proyecto, results_cache, actuals_*, audit_log); decisiones clave (**validación = `aleph_engine.schema.parse`** envuelto en Pydantic, sin duplicar; **approve = patrón del ETL** validar→calcular→snapshot+cache+audit atómico vía RPC plpgsql; **concurrencia optimista** If-Match/version→409; **roles Entra `admin`/`gerencia`** aplicados en el API + RLS deny-by-default como backstop; idempotencia con Idempotency-Key); **roadmap de 8 fases** por estrangulamiento (0 auth✅·1 lectura·2 escritura API·3 concurrencia·4 RLS·5 Ingreso en /web·6 corte Streamlit·7 actuals/EVM·8 multi-company). **FASE 1 HECHA:** `api/aleph_api/repo.py` ahora resuelve `slug → project → escenario BASELINE (o el approved de mayor versión) → snapshot` en vez de `proyectos.data`. **Clave:** el ETL (`db/etl_import_v1.py`, Fase 4b) pobló `scenarios` con v1 **`approved`** (NO baseline) y `snapshot` = el `par` COMPLETO bit a bit → leer de ahí devuelve el dato IDÉNTICO (dorado verde por construcción). **Seguridad:** `proyectos` queda de **espejo de respaldo** (si el modelo nuevo no responde → fallback, nunca se rompe) + palanca **`ALEPH_READ_SCENARIOS=false`** revierte sin redeploy. Test con cliente Supabase **falso** (`test_fase1_cut_over_lectura_a_scenarios`) cubre scenarios-first + rollback (no se puede probar contra el Supabase real: el MCP no lo alcanza y no hay creds locales). `.test.ps1` verde. **FASE 1 VERIFICADA EN PROD:** se añadió `read_model` a `/health/data` (verificar de verdad, no solo 200) → tras el redeploy, `{data_source:supabase, project_count:3, read_model:"scenarios"}` confirma que el API lee del modelo objetivo (no del espejo). **FASE 2 HECHA (escritura en el API):** `api/aleph_api/write.py` + 5 endpoints bajo `require_admin` (`POST /v1/projects`, `POST /v1/projects/{id}/scenarios`, `PUT /v1/scenarios/{id}`, `POST .../approve`, `POST .../baseline`). Cada mutación: valida con `schema.parse` (misma compuerta), valida transición (solo se edita `draft`; approved/baseline → **409**), audita, y al **aprobar** recalcula con `calcular()` → `results_cache` + devuelve TIR/checks (réplica del ETL). Snapshot bit a bit, motor intacto. Tests con **cliente Supabase falso ampliado** (insert/update/upsert + registro de llamadas): crear+audita, slug dup 409, par inválido 422, editar-solo-draft 409, aprobar recalcula+cache+audita (api **30** verdes). El path local da **503** (escritura exige Supabase). **GOTCHA de verificación de Fase 2 en prod:** los writes exigen el rol **`admin`** en el token → Martin debe crear **app roles `admin`/`gerencia`** en el app registration **del API** en Entra + asignarse (paso de portal); y el test real (crear proyecto por Swagger `/docs`) necesita un token → es **más natural verificarlo cuando llegue la UI de Fase 5** (formularios). **FASE 3 HECHA (endurecer):** migración **`db/migrations/0002`** (aditiva, Martin la corre en el SQL Editor): `updated_at` en scenarios + trigger que lo bumpea (ETag); trigger **`scenarios_inmutable`** que rechaza cambios a snapshot/version una vez fuera de `draft` (garantía DURA en la BD, no solo disciplina de app). `write.py editar_draft` acepta **`if_match`** → compare-and-swap (`.eq(updated_at, if_match)`); si la fila cambió → 409; `PUT` lee el header `If-Match`. api **32** verdes. **PENDIENTES de PROD (acumulados, los hace Martin):** (1) correr la **migración 0002** en Supabase, (2) **redeploy** del API (SHA con Fases 2+3), (3) crear **app roles `admin`/`gerencia`** en Entra + asignarse. **Siguiente:** Fase 4 (RLS granular) o saltar a **Fase 5 (formularios de Ingreso en `/web`)** — que además da la forma NATURAL de probar la escritura end-to-end (crear proyecto con clics). **Por qué importa:** el API de escritura está construido y endurecido (draft→approve→baseline, inmutable, anti lost-update) sin mover una cifra; falta exponerlo en la UI y los pasos de portal de Martin.
- **2026-06-13 — PARIDAD DE LECTURA esencialmente ALCANZADA en `/web` (5 tableros en un día) + gotcha TIR -99% greenfield + cada cambio de API exige redeploy manual:** En una sesión se desplegó el `/web` (Vercel + Entra) y se construyeron **5 tableros**: Cronograma, **WACC**, Ficha técnica (urbanístico), Heatmap 2D, y **Mapa de valor** (burbujas TIR×margen del portafolio). Con esto el lado de **CONSULTA tiene paridad casi total** con el Streamlit (Portafolio, Pipeline, Resumen+P&G+ficha técnica, Flujo, Cronograma, Costo de capital, Sensibilidad con escenarios/tornado/heatmap/MC, Mapa de valor). **GOTCHA TIR -99% (constitución: "jamás TIR −99%"):** Torres de Campiñas tiene `tir_proyecto = -99.4%` (IRR degenerado), pero `tir_apalancada_ref = 20%`. La función del motor `portfolio.puntos_burbujas` usa `tir_proyecto` → mostraría -99% y rompería la escala. **Solución (sin tocar el motor):** el Mapa de valor se construye desde los **items del portafolio** (que ya usan `tir_apalancada_ref or tir_proyecto`, consistente con la tabla) enriquecidos con `margen` en `build.portafolio`; y la UI **filtra** los puntos con tir/margen null (greenfield) mostrando "N excluidos". Para futuros gráficos de portafolio: **nunca graficar `tir_proyecto` crudo** (puede ser -99%); usar la TIR de los items o filtrar degenerados. **GOTCHA operativo recurrente:** Vercel **auto-despliega el `/web`** en cada push, pero el **API en Azure NO** — cada vez que un tablero añade/cambia un endpoint o campo del API (Cronograma `/schedule`, WACC `/wacc`, Mapa de valor `margen` en items) hay que **redeployar el API manualmente** (Cloud Shell: `az acr build ... --image alephapi:<SHA> --file Dockerfile.api ...#main` → `az webapp config container set ... --container-image-name ...:<SHA>` → `az webapp restart`, RG `Cg-factibilidad`). Verificación: ruta nueva sin token = **401** (existe) vs 404 (no desplegada). **Por qué importa:** queda registrado que la paridad de CONSULTA está lista (gran hito), el patrón anti-(-99%), y que **cada tablero con cambio de API arrastra un redeploy** (los UI-only que reusan endpoints existentes NO). **PENDIENTE para FULL parity + apagar Streamlit:** (a) curva S de costos (parcial, el desglose por capítulo es dato del comité/operativo), (b) EVM/Monitor (necesitan modelo `actuals`), (c) **camino de ESCRITURA** (Ingreso de datos: el `/web` es solo-lectura) = el habilitador real. **Inmediato:** el Mapa de valor necesita un redeploy del API (añadió `margen`) para poblarse en prod.
- **2026-06-13 — Tablero WACC hecho + qué tableros están BLOQUEADOS por falta de datos `actuals` (mapa para los próximos):** Segundo tablero de lectura: **Costo de capital (WACC)** — `build.wacc()` expone `finanzas.calcular_wacc(detalle=True)` (build-up CAPM Damodaran completo: beta des/reapalancada, Ke USD→+EMBI→paridad inflación→Ke COP, Kd, escudo fiscal, WACC=E·Ke+D·Kd(1−t)); **WACC Navarra 21.54%** es cifra dorada del `k.beta` (`test_wacc_fiel_al_motor`). UI **sin visx** (tablas + barra CSS de composición E·Ke/D·Kd) → **SÍ se captura en screenshots de agent-browser** (a diferencia de las gráficas visx). **CLASIFICACIÓN de los tableros que faltan por DISPONIBILIDAD DE DATOS** (clave para no chocar): **(a) LECTURA, datos ya en el motor — construibles ya:** WACC ✓, mapa de valor (burbujas, falta `margen` en items de portfolio), datos técnicos/urbanístico (ya en `getProject`), heatmap 2D (ya en `sensitivity.matriz_2d`), curva S de costos (ya en `results.distribucion`). **(b) BLOQUEADOS por falta de `actuals` (datos operativos): EVM (Valor Ganado) y Monitor de ejecución** — `evm.calcular_evm()` devuelve **None** porque **NINGÚN proyecto tiene `avance_real`/`costo_real`** en sus etapas (el grep lo confirma: solo en código/tests, cero JSON). El único avance REAL vive **hardcodeado en `app_streamlit/navarra_data.py`** (Streamlit-only: Torre 1 = 42.01% comité / 59.57% bancario; presupuesto base 22.910 · ejecutado 9.115 · proyectado 23.542 millones) que **cuadra** con el BAC top-down del motor (≈22.960) pero NO está en el API/Supabase. Para EVM/Monitor hay que **montar el modelo `actuals_*` de la constitución** (o sembrar `avance_real` en el proyecto) primero. **(c) ESCRITURA: Ingreso de datos** (crear/editar proyectos) — el `/web` es solo-lectura; es el habilitador real para apagar Streamlit, proyecto aparte. **Decisión con Martin:** EVM se DIFIERE hasta tener actuals; se priorizan los tableros de lectura (a). **Gotcha de datos menor:** el `rf` de Navarra sale 0.12% (muy bajo) pero el WACC cuadra al dorado → es el dato de origen, no bug; verificar el param si se quiere. **Por qué importa:** deja claro qué se puede construir ya vs qué espera el modelo de actuals, para no intentar EVM/Monitor sin datos.
- **2026-06-13 — Patrón para construir un TABLERO nuevo en `/web` (caso: Cronograma y Ventas) + restricción de usuarios en Entra + el plan Hobby de Vercel:** Se mapeó la **paridad módulo a módulo** (workflow de 3 Explore: Streamlit ~19 secciones vs API 8 endpoints vs `/web` 6 pantallas) y se construyó el tablero **Cronograma** (Gantt de etapas + curva de absorción + recaudo mensual) end-to-end **sin tocar el motor ni el dorado**. **PATRÓN CLAVE — el motor YA calcula casi todo; el trabajo es EXPONER, no recalcular:** `aleph_engine.calcular()` ya devuelve `R["hitos"]` (IV/PE/FV/IC/FC por etapa, fechas) y `R["recaudo"]` (separación/CI/subrogación + `por_etapa` con `ventas`/`entregas`/`offset`); el endpoint nuevo `GET /v1/scenarios/{id}/schedule` (`build.schedule()`) solo serializa eso (fechas→ISO + offset de mes desde `base_date`, agrega la absorción global sumando `por_etapa` desplazado por `offset`, recorta a la ventana activa). **Para un tablero futuro: primero revisar si el dato ya está en `R` (modelo.py `calcular()`); si sí, exponerlo en `build.py` + ruta en `main.py`; si no, ahí sí hay trabajo de motor.** **CHECK DE FIDELIDAD que cuadra solo:** la absorción reconcilia (`acum_ventas[-1] == unidades_total`) y `sum(recaudo.total) == ventas` (Navarra: 951 und, 229.7 mil M) → buen test de contrato (`test_schedule_fiel_al_motor`). **UI (cero cálculo en el front):** 3 gráficas a medida en **visx** siguiendo el patrón de `cash-flow-chart.tsx` (`ParentSize` + `scaleLinear` + tokens `var(--primary)`/`var(--cg-amber)`/`var(--rule)` + tooltip), helper `lib/timeline.ts` (eje temporal año/mes desde `base_date`), nueva pestaña en `ficha-tabs.tsx`, `getSchedule()` en `page.tsx` con **fallback graceful** (si el API no expone la ruta → "Cronograma no disponible", no crashea). **GOTCHA de verificación — agent-browser NO captura las gráficas visx:** las screenshots salen con la gráfica EN BLANCO (KPIs/leyendas/texto sí; iconos SSR sí) porque visx pinta el SVG **post-hidratación** vía `ParentSize`/ResizeObserver y el capturador no toma esa capa. **NO es un bug del código:** verificado con `agent-browser eval` (los rects/paths existen, posicionados en pantalla, `fill` OKLCH resuelto, ancestros `opacity:1`) y con experimento de control (la gráfica de **Flujo** preexistente sale igual de blanca). Para verificar gráficas visx: inspeccionar el **DOM por eval** (contar elementos, bbox, computed fill), no fiarse del screenshot. **DESPLIEGUE — ojo al orden:** el push auto-despliega el `/web` en Vercel, pero el **API en Azure (`cg-aleph-api`) NO se auto-despliega** (es manual por Cloud Shell, `api/DEPLOY.md`) → hasta redeployar el API, la pestaña Cronograma en prod muestra "no disponible" (fallback), aunque en local funciona. **Por qué importa:** queda el molde para los ~8 tableros que faltan (EVM, WACC, etc.): mapear paridad → ver si el dato está en `R` → exponer en build.py + test de reconciliación → 3 gráficas visx reusando el patrón → pestaña + fallback. **Restricción de usuarios (hecho con Martin):** en **Enterprise applications → ALEPH Web** (NO en App registrations) se asignan usuarios (plan gratis = usuarios individuales, grupos requieren P1) y luego Properties → "¿Asignación requerida? = Sí" → solo los asignados entran (resto: AADSTS50105); asignarse a uno mismo ANTES de activar. **Vercel Hobby:** gratis indefinido (sin caducidad), límites mensuales generosos (100 GB transfer / 6000 min build) que una app interna no roza; al exceder PAUSA 30 días (no cobra); es **uso no comercial** por términos → para producción formal conviene Pro (US$20/usuario/mes). **PENDIENTE:** Martin recupera GoDaddy (DNS de cgconstructora.com está en **GoDaddy**, NS `domaincontrol.com`) para el dominio propio `aleph.cgconstructora.com` (CNAME `aleph`→`cname.vercel-dns.com` + `AUTH_URL` + redirect URI en Entra) — pasos en el chat; y redeployar el API para encender Cronograma en prod.
- **2026-06-13 — `/web` DESPLEGADO Y VERIFICADO EN PRODUCCIÓN (login Entra → API Azure → dashboard) + gotchas REALES del portal que el runbook no anticipó del todo:** Se cerró el pendiente: Martin (no-dev) ejecutó los 4 bloques guiado paso a paso por capturas, y el `/web` quedó **vivo en `https://aleph-4iiv.vercel.app`** con login Microsoft real (cuenta @cgconstructora.com) mostrando el portafolio con cifras intactas (VPN @TIO 26.9 mil M, Navarra TIR apal. ref. 38.00%, etc., cada una con su etiqueta de base). **Verificación independiente (curl del agente):** `/` → 307 a `/login`, `/login` → 200, **sin candado SSO de Vercel** (la gerencia entra directo, solo login Microsoft); del lado API ya estaba `/v1/portfolio` sin token = **401**. **Valores reales de CG (NO secretos, son IDs públicos):** tenant `5c0ceba8-4397-49c8-a9fb-832860708337`, **App registration del API `ALEPH API`** = `5dccc7aa-8c5b-4a53-a2fe-4826062af4e5` (= `API_AUDIENCE` y base del scope), **App registration del Web `ALEPH Web`** = `e91870a2-71a0-4a1c-9c7b-9007d6161c4b` (= `AUTH_MICROSOFT_ENTRA_ID_ID`). **GOTCHAS REALES (registrados en `web/DEPLOY_WEB.md`):** (1) **el app registration del API NO existía** — hubo que CREARLO (`ALEPH API`, single-tenant, **sin** redirect URI); `cg-factibilidad-app` que aparecía es el del Streamlit, NO el del API. (2) **Confusión App Service vs App registration**: ambos se llaman `cg-aleph-api` pero son cosas distintas (servidor vs identidad) → el "Id. de aplicación (cliente)" SOLO vive en **Registros de aplicaciones**, no en la "Aplicación web". (3) **El manifiesto del portal estaba en formato AAD Graph**, no Microsoft Graph → el campo es **`accessTokenAcceptedVersion`** (en la RAÍZ), NO `requestedAccessTokenVersion`; pista para distinguir: si ves `replyUrlsWithType`/`oauth2Permissions` es AAD Graph. (4) **"Mis API" tardó en listar el API recién creado** (propagación de Entra) → atajo fiable: pestaña **"API usadas en mi organización" + buscar por el GUID**. (5) **VERCEL — el deploy falló (2 s, `framework=other`) porque el Root Directory quedó en `./`** aunque se eligió `web` en el import → arreglo: **Project → Settings → Build and Deployment → Root Directory = `web`** (+ Framework Preset = Next.js) → **Redeploy** (build real ~40 s). (6) Para pegar las 6 env en Vercel, **"Import .env" / paste de `KEY=VALUE`** evita typos en los GUID largos. **Por qué importa:** confirma el patrón completo de auth Entra para UIs nuevas y deja documentados los tropiezos del portal (el más caro: Root Directory `./` vs `web`, y el nombre del campo del token según formato de manifiesto). **PENDIENTES MENORES (opcionales, no bloquean):** Vercel está en plan **Hobby** (para uso comercial conviene **Pro** ~US$20/mes); dominio cripto `aleph-4iiv.vercel.app` (se puede poner uno propio + fijar `AUTH_URL`); si se quiere restringir quién entra, activar "asignación de usuarios requerida" en la Enterprise application de `ALEPH Web` (hoy entra cualquier cuenta del tenant).
- **2026-06-12 — PROMPT 5 · `/web` listo para Vercel con login Microsoft Entra ID (Auth.js v5), config-driven + 2 gotchas de Entra fundamentados:** Se dejó el `/web` desplegable a Vercel con login Entra **sin tocar el motor ni el dorado** (4 pasos, 1 commit c/u, `.test.ps1` verde: engine 29 / api 22 / app 73 / ruff). **Patrón clave — auth CONFIG-DRIVEN (espeja al API `aleph_api.auth`):** sin `AUTH_MICROSOFT_ENTRA_ID_ID` la auth queda **APAGADA** (dev local: el `/web` habla con el API local también abierto, 0 redirects) y definir las env de Entra en Vercel la **ENCIENDE** — cero cambios de código entre dev y prod. **Auth.js v5** (`next-auth@5-beta`, provider `microsoft-entra-id` que auto-infiere `AUTH_MICROSOFT_ENTRA_ID_{ID,SECRET,ISSUER}`); el `auth.ts` pide el scope del API (`ALEPH_API_SCOPE = api://<API_APP_ID>/access_as_user`) para que el `access_token` salga con la **audiencia del API**, y los callbacks `jwt`/`session` lo propagan. **Next 16 (breaking):** el *middleware* se llama ahora **`proxy.ts`** (export `proxy`, corre en Node) → chequeo **optimista** de la cookie de sesión (la validación real la hace `auth()` server-side + el API revalida el JWT); **route group `(app)/`** envuelve las rutas con el shell y `/login` queda fuera (URLs intactas). `api.ts` pasa a **server-only**: `apiFetch` adjunta `Authorization: Bearer` (vía `auth()`) y en **401/403 redirige a `/login?reason=expired`** (pantalla clara, sin auto-signIn → evita bucles). **DOS GOTCHAS de Entra que sin grounding (MS Learn MCP) se habrían ido a prod rotos:** (1) **`accessTokenAcceptedVersion=2`** en el manifiesto del **app registration del API** — con `null`/`1` los tokens salen **v1** con issuer `https://sts.windows.net/<tenant>/` y el API (que valida issuer **v2**) da **401 a todo**; (2) en tokens **v2 el claim `aud` ES el Application (client) ID del API (un GUID), NO `api://...`** (eso es v1) → en el App Service `API_AUDIENCE` debe ser el **GUID**. Se necesitan **2 app registrations** (API expone `access_as_user`; **Web nueva** con redirect URI `…/api/auth/callback/microsoft-entra-id` + secret + permiso delegado al API + **grant admin consent**). **vercel.json NO hace falta** (Root Directory=`web` se fija en el dashboard de Vercel; Next se auto-detecta); `.gitignore` necesita `!.env.example` porque el patrón `.env*` lo ocultaba. Todo en **`web/DEPLOY_WEB.md`** (valores exactos, comandos `az` para Cloud Shell, verificación end-to-end con `curl /v1/portfolio` sin token = 401, rollback = *Promote to Production* del deploy anterior, tabla AADSTS/401/503). **Por qué importa:** queda un patrón reutilizable de auth Entra para cualquier UI nueva (config-driven, fail-safe en dev), y los 2 gotchas v2/audiencia son la causa #1 de 401 silenciosos. **PENDIENTE (lo hace Martin, requiere portal Azure):** crear el app registration `ALEPH Web`, setear las env en Vercel + en `cg-aleph-api` (`API_AUDIENCE`=GUID del API, `ALEPH_AUTH_REQUIRED=true`, `accessTokenAcceptedVersion=2`) y desplegar.
- **2026-06-12 — PROMPT 5 · `/web` (Next.js) arrancado: dashboard + ficha + gráficas visx, con sistema de diseño anti-IA:** Se construyó `/web` (la UI nueva de ALEPH) consumiendo el API de LECTURA, **cero cálculo en el frontend** (cifras 1:1 del motor). **Stack:** Next.js **16.2.9** (App Router, Server Components, `params` es **Promise** → `await params`) + React 19 + **Tailwind v4** (config en CSS: `@import "tailwindcss"` + `@theme` + `@custom-variant dark`) + visx. **OJO Next 16:** `web/AGENTS.md` advierte que es una versión con breaking changes → **leer `web/node_modules/next/dist/docs/` antes de codear** (CSS/fonts/data-fetching confirmados). **Hosting decidido: Vercel** (Pro ~US$20/mes; la identidad sigue en Microsoft vía Entra/NextAuth; la gerencia que ve los tableros NO necesita cuenta Vercel). **Sistema de diseño** (`web/PRODUCT.md`+`web/DESIGN.md`, skill `impeccable`): tokens **OKLCH** tintados al teal CG #0E5E59 (sin #000/#fff), light primario + dark por clase `.dark` sin FOUC (script pre-paint), Inter, `tabular-nums`. **Crítica multi-lente (workflow de 4 skills de diseño: high-end-visual-design + emil-design-eng + design-taste-frontend + redesign-existing-projects)** elevó el look "muy IA" → artesanal: número-héroe con tracking negativo + **unidad de-enfatizada** (`Figure`: "$601.0" grande, "mil M" chico), hairlines tintadas al teal (`--rule`) vs borde de marco (`--border`), sombra teal (nunca shadow-md), **radio casi-recto (3px) en superficies de DATOS / pill solo para estado**, el punto ALEPH → **anillo de precisión teal** (`AlephMark`, único marcador de jerarquía + favicon), easing propio `var(--ease-out)`. **Gráficas a medida en visx** (decisión de Martin: "máximo a medida"): flujo de caja mensual (área acumulada + exposición máx anotada + toggle proyecto/inversionista), escenarios (barras Opt/Base/Pes), **tornado** divergente, **Monte Carlo interactivo** (histograma con barras bajo el hurdle en rojo + P10/P50/P90 + prob de éxito, vía **Server Action** → preparado para Entra). Pantallas: **dashboard de Portafolio** (`/`, StatPanel + embudo + tabla) y **ficha** (`/proyectos/[slug]`, pestañas Resumen/Flujo/Sensibilidad). **GOTCHA agent-browser (screenshots):** el primer `agent-browser open` tras un `close` **se cuelga** relanzando Chromium → patrón que funciona: dejar que se cuelgue, **TaskStop**, y luego comandos directos (`snapshot -i` para refs → `click @eN` → `screenshot`); **NUNCA `wait --load networkidle`** (Next mantiene conexiones → nunca asienta). Reiniciar el server de prod (`next start`) tras un rebuild **no recarga** el build viejo: hay que **matar el `next-server` por puerto** (TaskStop mata el wrapper npm, no el hijo node → `Get-NetTCPConnection -LocalPort 3000 | Stop-Process`). **Por qué importa:** queda la base reutilizable de `/web` (sistema de diseño + componentes canon) que heredan todas las pantallas siguientes; cada cifra con etiqueta de base; el deploy del `/web` a Vercel + login Entra está PENDIENTE (sigue en localhost). **DECISIÓN Monte Carlo:** el plan institucional de Martin (distribuciones log-normal/beta/triangular + matriz de correlaciones Cholesky + VaR/CVaR/fan/stress) es **trabajo de MOTOR** (`aleph_engine`), NO una gráfica web, y depende de **calibración con datos de CG** (CRM/Dominica = parcial) → se hizo la VISUALIZACIÓN sobre el MC actual del motor (uniforme, 3 variables, etiquetado honesto) y el motor institucional queda como **proyecto dedicado futuro** (con golden tests para no romper el dorado).
- **2026-06-12 — PROMPT 4 · ROTACIÓN de la service_role COMPLETADA (cierra el pendiente histórico de seguridad) + cómo se rota en Supabase HOY:** Se rotó la `service_role` filtrada con **CERO downtime** y queda **CERRADO** el pendiente que arrastraban varios aprendizajes ("rotar SUPABASE_KEY service_role expuesta"). **Hallazgo clave (grounded en docs de Supabase + verificado adversarialmente con un workflow de 3 agentes):** las llaves **legacy** `anon`/`service_role` (JWT firmados por un único JWT secret) **YA NO se pueden rotar/regenerar** ("it is no longer possible to rotate the legacy anon/service/JWT secrets"; además regenerar el JWT secret invalidaría anon+service_role A LA VEZ y cerraría TODAS las sesiones de usuario = downtime). El camino correcto es **migrar a las nuevas "Secret API keys"** (`sb_secret_…`, opacas, NO JWT) que **coexisten** con las legacy: el dashboard ya traía una `sb_secret` `default` (Settings→API Keys→"Publishable and secret API keys") → poner ese valor en los **3 almacenes** de `SUPABASE_KEY`: (1) `app_streamlit/.streamlit/secrets.toml` local, (2) app setting del App Service **`cg-factibilidad-app`** (Streamlit), (3) app setting de **`cg-aleph-api`** (API) — ambos en RG `Cg-factibilidad` → **REINICIAR ambos App Services** (storage.py cachea el cliente en global; repo.py usa `@lru_cache` → la env var solo entra en un (re)arranque) → **verificar AMBOS con la nueva** (API `/health/data` → `project_count:3`; Streamlit Navarra **37.60%** real — si cayera a ilustrativo ~7.3% sería fallback) → SOLO ENTONCES **Disable** de las legacy (Settings→API Keys→pestaña "Legacy anon, service_role"→escribir `disable`→confirmar). **Disable es REVERSIBLE** y corta el uso como `apikey` contra la BD (PostgREST), que es el riesgo real; el aviso *"they remain valid as a JWT"* significa que para matarla también a nivel JWT haría falta el paso **OPCIONAL** de migrar a **JWT signing keys** asimétricas (ES256) + Revoke (no hecho, no necesario hoy). Re-verificado tras el Disable: `/health/data` sigue `project_count:3` → nada dependía de la legacy. **CERO cambios de código** (la key nunca estuvo hardcodeada; se lee de secrets/env; las `sb_secret` opacas funcionan igual en el header `apikey`). **Gotcha de input ya conocido:** la key se mete con `SBKEY=` + pegar + Enter (sin `<...>`), y se reusa `--settings SUPABASE_KEY="$SBKEY"` para ambos servicios en la misma sesión. **Por qué importa:** patrón de rotación SIN downtime para este proyecto + el pendiente de seguridad histórico queda CERRADO. **Higiene menor restante:** la key vieja sigue en transcripts/historiales pero está DESACTIVADA (inútil para datos).
- **2026-06-12 — PROMPT 4 · Fase 4d: el API se desplegó VIVO pero servía 0 proyectos → endurecimiento (verificación independiente + auditoría adversarial):** El App Service `cg-aleph-api` se creó y `/version` respondió OK, pero una verificación **INDEPENDIENTE** (curl del agente directo a la URL pública, no vía Martin) destapó que `/v1/portfolio` daba **`n:0`** — la API leía CERO proyectos. Causa raíz (confirmada por un workflow de 4 agentes: root-cause + grounding en MS Learn + audit + verificación adversarial): **(1) GOTCHA central: la imagen del API NO trae JSON de respaldo** (`Dockerfile.api` solo COPIA `engine/` + `api/aleph_api/`, nunca `app_streamlit/`; `repo._repo_root()` exige `engine/`+`app_streamlit/` juntos → en el contenedor devuelve None → fallback local vacío). Por eso **en prod Supabase es OBLIGATORIO**; sin config válida la API servía **200 con portafolio vacío EN SILENCIO** (repo.py traga la excepción de Supabase y cae al fallback local inexistente → `[]`). **(2) El `az webapp config appsettings set` nunca se aplicó:** el bloque del runbook tenía `SUPABASE_KEY=<pega-aquí…>` y los símbolos `< >` bash los interpreta como **redirección** → `syntax error near unexpected token newline` → las env vars no se setearon. **Arreglos (commit `175cec2`, requieren redeploy):** (a) endpoint público NO sensible **`GET /health/data` → {data_source, project_count}** que verifica el deploy (project_count>0) **sin exponer `/v1`**; (b) **`ALEPH_DATA_REQUIRED=true`** (espejo de `ALEPH_AUTH_REQUIRED`) → `/v1/portfolio` da **503** en vez de 200-vacío cuando no hay datos (fail-loud); (c) runbook usa **`read -s`** para la service_role (oculta, sin historial, sin romper bash); (d) **flags `az` vigentes** en `az webapp create` (grounded vs learn.microsoft.com): `--deployment-container-image-name`→`--container-image-name`, `--docker-registry-server-user/password`→`--container-registry-user/password`, `--docker-registry-server-url`→`--container-registry-url` (`az webapp config container set` YA usaba los vigentes); (e) cotas de versión mayor (`<N`) en las deps del `Dockerfile.api`; (f) los scripts verifican DATOS, no solo `/version`. api **22 verdes**. **Por qué importa (meta):** un deploy "verde" por `/version` 200 es un **falso positivo** si no toca datos — para un API hay que verificar **n>0**, no "responde 200"; y la **verificación independiente + auditoría adversarial con workflow** atrapó un fail silencioso que se habría dado por bueno. **DEPLOY 4d VERIFICADO** (SHA `8af47ec`, App Service `cg-aleph-api` en el mismo RG/plan/ACR del Streamlit): `/health/data` → `{data_source:supabase, project_count:3}` (lee Supabase con la service_role; cifras intactas). **GOTCHA de input en Cloud Shell:** el primer intento de `az ... appsettings set SUPABASE_KEY=<...>` falló por los `< >` (redirección bash); el patrón robusto para un no-dev fue **`SBKEY=` + pegar la key (JWT = solo `[A-Za-z0-9._-]`, seguro SIN comillas) + Enter**, y luego `--settings SUPABASE_KEY="$SBKEY"`. **Pendiente:** (1) **bloquear `/v1` YA** con `ALEPH_AUTH_REQUIRED=true` (sin Entra → **503** fail-closed): cierra la exposición de cifras REALES con **cero downside** porque aún no hay consumidor de `/v1`; (2) registrar la app en Entra (Parte A del runbook) **cuando se construya `/web`**, para abrir `/v1` a usuarios autenticados; (3) **ROTAR la `service_role`** (se volvió a exponer, esta vez en el chat de la sesión).
- **2026-06-12 — PROMPT 4 · Fase 4b EJECUTADA (migración corrida sobre la BD de la app) + Fase 4d PREPARADA (deploy del API):** Martin ejecutó la migración por el **camino A** (autocontenido, sin MCP): pegó `db/migrations/0001_aleph_schema.sql` en el SQL Editor de su Supabase (`jehkdhmngxvuvhxuhlan`) → **"Success. No rows returned"** (es DDL: 0 filas es el éxito esperado, no un error) y corrió `.\migrate_db.ps1` → **3 proyectos importados, 0 inválidos**; gate dorado VERDE: **Navarra `tir_proyecto=0.375975` (=37.60%, idéntico a `app_streamlit/tests/golden/1_navarra_REAL_snapshot.json`)**, Dominica 0.565495. Confirma en la práctica la garantía de diseño (snapshot bit-a-bit + recálculo con el MISMO motor → cifras imposibles de mover). El MCP de Supabase de Claude **seguía sin alcanzar esa BD**, así que el ETL con la `service_role` (la que ya usa la app) fue el desbloqueo correcto. **4d preparado y pusheado** (`481736a`): `Dockerfile.api` (bundlea engine/ + api/aleph_api/, uvicorn :8000; `/version` PÚBLICO = health check REAL porque el API **NO usa Easy Auth**, a diferencia del Streamlit) + **arreglo de `.dockerignore`** (excluía `api/` entero → el build del API habría fallado al copiar `api/aleph_api`; ahora la cruft la cubren los patrones generales y `Dockerfile.streamlit` no copia `api/`) + `deploy_api.ps1/.sh` (tag=SHA, captura imagen previa para rollback, health check sobre `/version`; primer deploy detecta App Service ausente e imprime la provisión) + `api/DEPLOY.md` (runbook Cloud Shell + build desde GitHub, mismo método probado del Streamlit; App Service NUEVO `cg-aleph-api` en el mismo RG/plan B1/ACR). Smoke-probado sin Docker: engine instala, `aleph_api.main` importa (11 rutas), **19 tests del API verdes**. **Decisión con Martin:** primer deploy con **auth APAGADA** (solo `SUPABASE_URL/KEY`+`WEBSITES_PORT`) para validar lectura, luego **cerrar fail-closed** con Entra (Parte B-2 del runbook: `ENTRA_TENANT_ID`+`API_AUDIENCE`+`ALEPH_AUTH_REQUIRED=true`). **Por qué importa:** 4b pasa de "preparada" a HECHA (el bloqueo del MCP ya no frena nada); y el `/version` público del API permite por fin un health check de deploy automatizable (el del Streamlit no, por Easy Auth). **Pendiente PROMPT 4:** Martin corre el deploy del API por Cloud Shell (`api/DEPLOY.md`) + cut-over de `storage.py`/`repo.py` a leer de `scenarios` (fase posterior, más delicada).
- **2026-06-12 — PROMPT 4 · Fase 4c: auth Entra ID en la API (JWT) + revisión adversarial de seguridad:** `api/aleph_api/auth.py` valida el JWT de Entra ID con **PyJWT** (firma RS256 vía JWKS por `kid`, `aud`, `iss`, `tid`, `exp`), fundamentado en la guía oficial de Microsoft (grounding con el MCP de Microsoft Learn). Es **config-driven**: se activa con `ENTRA_TENANT_ID`+`API_AUDIENCE`; rutas `/v1/*` protegidas por un router con `Depends(current_user)`, `/version` público. Tras implementar, se corrió una **revisión adversarial con un Workflow** (3 lentes: validación JWT / bypass-misconfig / seguridad de API) → **12 hallazgos**, se aplicaron los reales: (1) **FAIL-CLOSED** — `ALEPH_AUTH_REQUIRED=true` (a poner en PROD) hace que `current_user` devuelva **503** si la auth no está configurada, en vez de abrirse silenciosamente (el patrón "abierto en dev" era cómodo pero peligroso en prod); (2) **validar `tid`** == tenant (defensa cross-tenant, aunque el `iss` ya lo incluye); (3) leeway 60→**30 s**; (4) mensaje 401 **genérico** (no filtrar la clase de excepción PyJWT); (5) quitar el estado de auth del `/version` público; (6) **logging** en los `except` de `repo.py`/`build.py` (no enmascarar fallos de Supabase); (7) `oid` para auditoría; (8) allowlist opcional de `azp`/`appid` (`ALEPH_ALLOWED_AZP`). Tests con **clave RSA de prueba** (sin tocar Entra): válido pasa; rechaza expirado/aud/iss/tid/alg-confusion(HS256)/firma-inválida + fail-closed → **api 19 verdes**. `pyjwt[crypto]` cableado en CI/`test.sh`. **Aprendizaje meta:** para código sensible a seguridad, el patrón **implementar → revisar adversarialmente con workflow (lentes diversas) → aplicar hallazgos** atrapó un fail-open real que sin la revisión se habría ido a prod. **Pendiente PROMPT 4:** 4b migración de datos (Martin conecta la BD al MCP) · 4d deploy `cg-aleph-api` (Cloud Shell + build desde GitHub).
- **2026-06-12 — PROMPT 4 · Fase 4a: API FastAPI de LECTURA sobre los datos actuales (sin tocar Supabase ni el Streamlit):** Se montó `/api` (`aleph_api`, FastAPI, v0.1.0) que **reutiliza `aleph_engine`** (cero fórmulas nuevas): `repo.py` lee los proyectos con el MISMO patrón que `storage.py` (tabla `proyectos` si hay `SUPABASE_URL/KEY`, si no JSON locales con `*_REAL.json` con prioridad → en dev/CI funciona sin Supabase); `build.py` arma los payloads del §5 desde `calcular()`+`metrics`+`checks`+`portfolio`+`modelo`, añadiendo **etiqueta de base** (`metrics.etiqueta`, TIR proyecto ≠ TIR socio) y `checks`; `main.py` expone `/version`, `/v1/portfolio`, `/v1/projects/{slug}`, `/v1/scenarios/{slug}:base/results|sensitivity`, `POST …/run` (Monte Carlo). **Test de contrato dorado** (`api/tests`, 6 verdes): el `GET …/results` de Navarra da **TIR proyecto 37.60%** (datos REALES, solo local; en CI se salta) **y** la API expone EXACTAMENTE lo que produce el motor (fidelidad: `ind == R['apalancamiento']`). Cableado: CI y `test.sh/.ps1` ahora corren **engine+api+app** (instalan `fastapi httpx`); `dev_api.sh/.ps1` levanta `uvicorn` en `:8000/docs`. **GOTCHA de empaquetado:** `api/pyproject.toml` declara `aleph-engine` como dep → NO hacer `pip install ./api` suelto (intentaría bajar `aleph-engine` de PyPI y falla); en dev/CI se usa `pip install -e ./engine` + `pythonpath=["."]`; en el futuro Docker del API: instalar `./engine` primero. **Decisiones con Martin:** arrancar por 4a (lo más seguro); para **4b** (migración a `projects`/`scenarios`) Martin **conectará la BD correcta (`jehkdhmngxvuvhxuhlan`) al MCP de Supabase** (el actual no la alcanza). **Pendiente:** 4b migración de datos · 4c auth Entra ID + roles · 4d deploy (`cg-aleph-api`, tag=SHA, `/version`).
- **2026-06-12 — PRIMER DEPLOY del monorepo OK (prod corre el motor empaquetado) + Supabase MCP NO alcanza la BD de la app:** El primer deploy del contexto monorepo **se desplegó y verificó en producción**: la app carga y las cifras de Navarra siguen intactas → `aleph_engine` empaquetado corre en Azure. **El camino que FUNCIONÓ no fue `deploy_streamlit.ps1`** (Martin no tiene `az` local; Cloud Shell no tiene los archivos locales) **sino un build desde GitHub en Azure Cloud Shell** (el repo `aleph` es PÚBLICO): `az acr build --registry cgfactibilidadacr --image cgapp:<SHA> --file Dockerfile.streamlit https://github.com/DjmartinG/aleph.git#main` → `az webapp config container set --container-image-name cgfactibilidadacr.azurecr.io/cgapp:<SHA>` → `az webapp restart`. **Verificar la imagen viva:** `az webapp config container show … --query "[?name=='DOCKER_CUSTOM_IMAGE_NAME'].value | [0]"` devuelve `DOCKER|…/cgapp:<SHA>` (el pie de la app NO sirve para distinguir: ambas imágenes dicen v2.39.0; prod pasó de `:latest` a tag=SHA). **Por qué importa:** el deploy real de este proyecto se hace por **Cloud Shell + build desde GitHub** (no contexto local); los `deploy_streamlit.*` sirven si algún día hay `az` local. **GOTCHA PROMPT 4:** el **MCP de Supabase conectado a Claude NO tiene permiso** sobre la BD de la app (`jehkdhmngxvuvhxuhlan`); solo ve otra organización → la migración de esquema (projects/scenarios) NO se puede hacer por MCP: hay que conectar la BD correcta al MCP, o hacer el ETL con la `service_role` key (como `storage.py`).
- **2026-06-12 — Deploy: el agente NO tiene `az`, y Easy Auth invalida el health-check anónimo:** Al ir a hacer el primer deploy del contexto monorepo se descubrió: (1) **el entorno del agente (Bash y PowerShell de la herramienta) NO tiene `az`** en el PATH ni en las rutas típicas → **el deploy lo corre Martin** en su terminal (`.\deploy_streamlit.ps1`, con su sesión `az login`), no el agente. (2) **GOTCHA real:** la app corre tras **Easy Auth (login de Microsoft)**, así que `curl/Invoke-WebRequest` a `$URL/_stcore/health` devuelve la **página de login** (200/302) AUNQUE el contenedor esté roto → **falso positivo**. Se corrigió la verificación de `deploy_streamlit.*`: en vez de health-check anónimo, **espera ~40s + consulta `az webapp show --query state`** y **exige confirmación visual en el navegador** (pie «Aplicativo vX.Y.Z» + cifras de Navarra), con el comando de **rollback** siempre impreso (captura `PREV_IMG` antes de cambiar) y `az webapp log tail` para depurar. **Por qué importa:** (a) ninguna automatización de deploy del agente puede asumir `az` disponible; (b) detrás de Easy Auth, "responde HTTP 200" ≠ "la app arrancó" — verificar por estado del App Service + ojo humano logueado.
- **2026-06-12 — PROMPT 3 COMPLETO: desacoplar los 7 puntos de `app.py` → motor (ninguna fórmula financiera ya en la app):** Se extrajeron los 7 puntos acoplados (§3.2 paso 13), **copiando logica verbatim** y con el dorado + 73 tests como red (commit por bloque): (1) `_irr_anual` dup → `finanzas.irr_anual_biseccion` (NO se cambió a `irr_anual`/brentq: el `_irr_anual` anualiza una bisección mensual, distinto método; swap habría movido la TIR equity del consolidado, que **no tiene cobertura del dorado**); (2-4) `consolidado`/`puntos_portafolio`/`pipeline_datos` → `aleph_engine/portfolio.py` (`consolidar`/`puntos_burbujas`/`pipeline`), recibiendo `items=[(slug,par,R)]`; la app quedó como **cliente delgado** (carga + delega) y formatea las `filas` al render; (5) la regla "si el waterfall corrió, sus cifras de decisión mandan sobre flujo_caja" → `metrics.flujo_decision(R)` (selección de fuente, no cálculo); (6) heatmap 2D de margen → `modelo.heatmap_sensibilidad(par, pasos)`. **JUICIO EXPERTO en el punto 7 (EVM):** el "EVM real de Navarra" que el plan marcaba como **duplicado** NO lo es — son **dos metodologías distintas**: `evm.calcular_evm` es **top-down** del modelo (curva planeada + avance por etapa, `EAC=BAC/CPI`), mientras el control de presupuesto del comité es **bottom-up** (partidas reales, `EAC=Σ proyectado_por_partida`). Forzar el merge habría **cambiado el EAC del comité** (cifra real). Solución correcta: extraer solo la **fórmula EVM compartida** a `evm.indices(BAC,AC,EV/avance,PV,EAC)` y usarla en AMBOS lados — el EAC bottom-up se **pasa** y se respeta; cero cifras cambiadas. (Mismo patrón que el aprendizaje de NO fusionar `flujo_caja`/`flujo_apalancado`.) Cobertura nueva: `test_portfolio`, `test_app_extraidos`, `test_evm` + `engine/tests` ahora **29 verdes**; app **73**. Como ninguna de estas funciones cambia la salida de `calcular()`, el **dorado sigue intacto** (agregar capacidades al motor no rompe el snapshot por-proyecto). **PROMPT 3 cerrado** (motor extraído + metrics + checks + app desacoplada). **Siguiente:** primer deploy del contexto monorepo (estrenar imagen) + PROMPT 4 (API FastAPI).
- **2026-06-12 — Sacar el repo de OneDrive + montar el monorepo en `C:\Code\aleph`:** OneDrive sincronizando la carpeta `.git` la **corrompe** (apareció un `index.lock` colgado a media operación de git). **Solución:** mover el código FUERA de OneDrive vía **clon fresco de GitHub** a `C:\Code\aleph` (no se arrastra el `.git` corrupto; GitHub es la fuente de verdad) + copiar lo **local-only** que no está en GitHub (`proyectos_privados/`, snapshots reales, `.streamlit/secrets.toml`). En el mismo movimiento se reestructuró al **monorepo ALEPH**: la app actual → `app_streamlit/` (con `git mv`, historial intacto); se crearon `engine/ api/ web/` (esqueletos) y los docs (`CLAUDE.md`/`AGENTS.md`/`directives/`) subieron al **root del monorepo** (ahora SÍ versionados). El CI (`ci.yml`) corre con `working-directory: app_streamlit`. La copia vieja en OneDrive queda de respaldo hasta verificar el deploy. **Por qué importa:** (1) **nunca** poner un repo git dentro de OneDrive/Dropbox (corrupción); (2) el `Dockerfile` ahora está en `app_streamlit/` → el `deploy_streamlit.sh` (PROMPT 2.4) debe usar contexto `#main:app_streamlit` con **tag=SHA**; (3) tras esto, Martin reabre VS Code en `C:\Code\aleph`. **Verificado:** 73 tests verdes (incl. dorado) desde la ubicación nueva.
- **2026-06-12 — PIVOTE ESTRATÉGICO: migrar de Streamlit a ALEPH (3 capas) por estrangulamiento:** Con Martin se decidió migrar de Streamlit a `/engine` (`aleph_engine`, Python puro) + `/api` (FastAPI) + `/web` (Next.js + TS + Tailwind + shadcn/ui), manteniendo el Streamlit **vivo en producción** hasta tener **paridad módulo a módulo** (NO big-bang). La **constitución gobernante** está arriba (§ALEPH). Arranque: **snapshot dorado automático** (las cifras de Navarra ya viven en `app_factibilidad/tests/test_anclas.py`) + extraer `cg_engine`→`aleph_engine` **tal cual** (no reescribir WACC/curva S/fiducia de memoria). **Por qué importa:** redefine TODO el plan — `NORTE_TABLEROS.md`/`REESTRUCTURACION.md` (Streamlit) pasan a histórico; deploy ahora **por SHA del commit**, no `:latest`; cada cifra en UI nueva lleva **etiqueta de base** (TIR proyecto/socio/ref, nunca "TIR" sola). **Pendiente decidir con Martin:** hosting de `/web` (Vercel vs Azure SWA por el "todo-Microsoft"), estructura del monorepo, y si se cierra primero el despliegue v2.39 de Streamlit.
- **2026-06-11 — Gotcha del Bash tool en Windows: NO usar here-strings de PowerShell (`@'...'@`):** El "Bash tool" corre **bash**, no PowerShell, aunque el entorno sea Windows. Pasar `git commit -m @'...'@` deja un `@` espurio al inicio y fin del mensaje (bash trata `@'` como literal + comilla simple). **Solución:** escribir el mensaje a un archivo (`.tmp/commitmsg.txt`) y `git commit -F` / `--amend -F`. **Por qué importa:** mensajes de commit/PR multilínea fiables sin contaminar el shell; el here-string `@'...'@` solo vale en el tool de PowerShell.
- **2026-06-10 — El ritmo de ventas mueve la TIR, no el margen (y expuso un bug de timing):** En el Monte Carlo de TIR/VPN (`engine.montecarlo_tir`), variar `vmes` cambia hitos PE→IC→FC y el recaudo, pero **NO el margen** (el margen solo depende de precio/costo, es total sin timing). Al simularlo apareció un resultado invertido (vender más rápido bajaba la TIR) porque la **escrituración** (offset fijo desde IV) no seguía a la obra: costos se adelantaban, el ingreso grande (subrogación) no. Fix: en la simulación, desplazar la escrituración por el mismo Δ de PE (mantener fija la brecha equilibrio→escrituración). Además, el MC **ignora el override de fiducia** (`par['fiducia']`) para que la TIR responda. **Por qué importa:** (1) si una variable solo afecta el timing, la salida del MC debe ser TIR/VPN, no margen; (2) sembrar `und=1` por etapa anula el efecto del ritmo (vende en el mes 0); (3) los shocks de un MC son un buen *test* de consistencia del modelo de tiempo.
- **2026-06-08 — Monte Carlo sin reescribir el motor:** `engine.montecarlo(par, n, rango_precio, rango_costo, seed)` itera `_correr()` (solo `pyg`, ~rápido: 500 sims <1s) y es determinista por `seed`. En la UI envolver en `@st.cache_data` keyado por `json.dumps(par, sort_keys=True)` + n + rangos (el dict no es hasheable). **Por qué importa:** análisis probabilístico (P10/P50/P90, prob margen>0) reutilizando la fuente única, con caché reproducible.
- **2026-05-28 — Python del equipo:** está en `C:\Users\Usuario\AppData\Local\Programs\Python\Python312\python.exe`, NO en PATH (los stubs de Microsoft Store lo sombrean). **Por qué importa:** invocar siempre por ruta completa en scripts y comandos.

> _Entradas anteriores a la migración (Streamlit · 2026-05/06) y sub-pasos ya superados: archivadas en [docs/historial_aprendizajes.md](docs/historial_aprendizajes.md) — no se borran, se archivan._

<!-- Agrega nuevas entradas arriba de esta línea. -->

---

Tú trabajas con una **metodología de 3 capas operativas** que separa responsabilidades para maximizar la confiabilidad. Los LLMs son probabilísticos, mientras que la mayoría de la lógica de negocio es determinista y requiere consistencia. Este sistema resuelve esa incompatibilidad.

## Metodología del Agente (capas operativas)

> ⚠️ Esto es **CÓMO** trabaja el agente (directiva → orquestación → ejecución). NO confundir con las 3 capas de **PRODUCTO** de ALEPH (`engine`/`api`/`web`, §ALEPH arriba), que es **QUÉ** construimos.

**Capa 1: Directiva (Qué hacer)**
- Básicamente son SOPs escritos en Markdown, ubicados en `directives/`
- Definen los objetivos, entradas, herramientas/scripts a usar, salidas y casos extremos
- Instrucciones en lenguaje natural, como las que le daría a un empleado de nivel medio

**Capa 2: Orquestación (Toma de decisiones)**
- Esta es tu función. Tu trabajo: enrutamiento inteligente.
- Leer directivas, llamar herramientas de ejecución en el orden correcto, manejar errores, pedir aclaraciones, actualizar directivas con los aprendizajes
- Tú eres el puente entre la intención y la ejecución. Por ejemplo, no inventes el ETL de migración por tu cuenta—lee `directives/plan_migracion.md`, define entradas/salidas y luego ejecuta el script determinista en `execution/`.

**Capa 3: Ejecución (Hacer el trabajo)**
- Scripts de Python deterministas en `execution/`
- Variables de entorno, tokens de API, etc. se almacenan en `.env`
- Manejan llamadas a APIs, procesamiento de datos, operaciones de archivos e interacciones con bases de datos
- Confiables, testeables, rápidos. Use scripts en vez de trabajo manual.

**Por qué funciona esto:** si tú haces todo por tu cuenta, los errores se acumulan. Un 90% de precisión por paso = 59% de éxito en 5 pasos. La solución es empujar la complejidad hacia código determinista. Así tú te concentras solo en la toma de decisiones.

## Principios de Operación

**1. Revise primero si existen herramientas**
Antes de escribir un script, revisa `execution/` según tu directiva. Solo crea scripts nuevos si no existe ninguno.

**2. Auto-corrección cuando algo falla**
- Lee el mensaje de error y el stack trace
- Corrige el script y pruébalo de nuevo (a menos que use tokens/créditos de pago—en ese caso consulta primero con el usuario)
- Actualiza la directiva con lo que aprendiste (límites o rate limits de API, tiempos, casos extremos)
- Ejemplo: si llegas al rate limit de una API → investigas la API → encuentras un endpoint batch que soluciona el problema → reescribes el script → pruebas → actualizas la directiva.

**3. Actualice las directivas a medida que aprende**
Las directivas son documentos vivos. Cuando descubras restricciones de API, mejores enfoques, errores comunes o expectativas de tiempo—actualiza la directiva. Pero no crees ni sobreescribas directivas sin preguntar, a menos que se te indique explícitamente. Las directivas son tu conjunto de instrucciones y deben preservarse (y mejorarse con el tiempo, no usarse de manera improvisada y luego descartarse).

## Ciclo de Auto-corrección

Los errores son oportunidades de aprendizaje. Cuando algo falla:
1. Corrija el problema
2. Actualice la herramienta
3. Pruebe la herramienta, asegúrese de que funcione
4. Actualice la directiva con el nuevo flujo
5. El sistema ahora es más robusto

## Organización de Archivos

**Estructura de directorios:**
- `.tmp/` - Todos los archivos intermedios (dossiers, datos scrapeados, exportaciones temporales). Nunca se suben al repositorio, siempre se regeneran.
- `execution/` - Scripts de Python (las herramientas deterministas).
- `directives/` - SOPs en Markdown (el conjunto de instrucciones).
- `.env` - Variables de entorno y claves de API.

**Principio clave:** Los archivos intermedios viven en `.tmp/` y pueden borrarse siempre. Cualquier salida del flujo debe ser reproducible ejecutando el flujo de nuevo, nunca editada a 
mano.


## Resumen

Tú estás entre la intención humana (directivas) y la ejecución determinista (scripts de Python). Lee instrucciones, toma decisiones, llama herramientas, maneja errores y mejora el sistema continuamente.

Se pragmático. Se confiable. Auto-corrígjete.

